<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Lucia</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://lupeipei.github.io/"/>
  <updated>2019-09-08T12:48:36.633Z</updated>
  <id>http://lupeipei.github.io/</id>
  
  <author>
    <name>Lucia</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>shell入坑笔记</title>
    <link href="http://lupeipei.github.io/2019/09/08/notes-of-shell-programming-in-unix-linux-and-osx/"/>
    <id>http://lupeipei.github.io/2019/09/08/notes-of-shell-programming-in-unix-linux-and-osx/</id>
    <published>2019-09-08T11:28:37.000Z</published>
    <updated>2019-09-08T12:48:36.633Z</updated>
    
    <content type="html"><![CDATA[<p>算是<a href="https://www.amazon.com/Shell-Programming-Unix-Linux-Developers-ebook/dp/B01L480ILM" target="_blank" rel="noopener">《Shell Programming in Unix, Linux and OS X》forth edition</a>的读书笔记了。</p><p>如果想要更快入坑，强烈推荐Derek Banas 的1小时视频 <a href="https://www.youtube.com/watch?v=hwrnmQumtPw&amp;list=PLwidFWzbt81iYkUtN1MXxb7htI48h_MMp&amp;index=3&amp;t=0s" target="_blank" rel="noopener">Shell Scripting Tutorial</a>。</p><a id="more"></a> <h4 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h4><p>shell是什么？</p><blockquote><p>shell is simply a program that reads in the commands you type in and converts them into a form more readily understood by the system.</p></blockquote><p>就是一个程序program，详细来说，就是读取用户输入的命令，将其转换成系统更容易理解的形式。nothing more, nothing less.</p><p>shell‘s responsibilities：</p><ul><li>Program execution</li><li>variable and filename substitution</li><li>I/O redirection</li><li>Pipeline hookup</li><li>Environment control</li><li>Interpreted programming language</li></ul><h4 id="基础命令"><a href="#基础命令" class="headerlink" title="基础命令"></a>基础命令</h4><p>一些基础的命令，比如：</p><ul><li><p>date</p></li><li><p>who</p></li><li><p>echo</p></li><li><p>ls</p></li><li><p>cat</p></li><li><p>wc：统计文件字数等</p><p>wc 有三个常用的option，也很好记。-l :统计行数(line), -c 统计字符数（char)， -w 统计字数(word)</p><p>当wc不带option时，会输出line，char, word的统计数</p></li><li><p>cp</p></li><li><p>mv</p><p>cp,mv有一个需要注意的地方，当目标文件已经存在时，会被替换。比如已有文件old_file，执行：</p><pre><code class="shell">cp file1 old_file# 或者mv file1 old_file</code></pre><p>都会导致原来存在的old_file文件被覆盖掉。</p></li><li><p>rm</p></li><li><p>pwd</p></li><li><p>cd</p></li><li><p>mkdir，rmdir</p><p>rmdir 删除的文件夹，需要是空的文件夹，不然会报错。一般使用<code>rm -r dir</code></p></li><li><p>ln</p><pre><code class="shell">ln from to</code></pre><p>建 symbolic links：</p><pre><code>ln -s from to</code></pre><p>Symbolic link 与普通link的区别是，symbolic link 指向原始文件，如果原文件被删除，link后的文件也就无效了，因为创建的link文件就是一个指向原文件的link而已。但是普通的link在原始文件被删除后，依然存在，也依然有效。</p></li><li><p>Standard input &amp; output redirection</p><p>标准输入重定向使用 <code>&lt;</code>, 输出重定向使用 <code>&gt;</code>:</p><pre><code class="shell">who &gt; userswc -l &lt; users# 输出5</code></pre><p>如果是向文件中追加内容，使用<code>&gt;&gt;</code>:</p><pre><code class="shell">echo hello &gt; testecho world &gt;&gt; testcat test# 输出helloworld</code></pre></li><li><p>standard error redirection</p><p>标准错误重定向，使用<code>command 2&gt;file</code>, 在一些脚本里面会可能会看到这样的命令：</p><pre><code class="shell">source ~/.bash_profile 2&gt; /dev/null </code></pre><p>这里<code>/dev/null</code>是系统的”garbage can”，你可以把它想象成是一个黑洞，任何不需要输出的内容都可以扔进去。</p><p>其他比如：</p><pre><code class="shell">&amp;&gt;/dev/null # 标准输出和错误都重定向到/dev/nullecho &quot;errors&quot; &gt;&amp;2 #将标准错误重定向到STDERR指定的文件curl -A curl -s github.com &gt; /dev/null 2&gt;&amp;1 #将标准错误重定向到标准输出，然后扔给/dev/null</code></pre></li><li><p>命令后台执行</p><p>使用&amp;， 例子:</p><pre><code>sort bigdata &gt; out &amp;</code></pre></li><li><p>ps</p><p>查看系统进程情况。</p></li></ul><h4 id="常用tools"><a href="#常用tools" class="headerlink" title="常用tools"></a>常用tools</h4><p>之前写过一篇，详细介绍cut， sed, tr, paste 的使用，这里略过这四位爷。</p><ul><li><p>cut 「略」</p></li><li><p>paste「略」</p></li><li><p>sed「略」</p></li><li><p>tr「略」</p></li><li><p>grep</p><p>查找任何匹配指定模式的内容。</p><p>基本样式：</p><pre><code class="shell">grep pattern files</code></pre><p>例子：</p><pre><code class="shell"># 找 pumaps | grep puma # 从 checklist 中 找 foodgrep food checklist# pattern最好套上单引号，比如# 从 checklist 中 找 * 字符，如果不使用单引号，则*会先被执行成当前目录下的文件列表，然后同checklist 一起作为参数传给grepgrep &#39;*&#39; checklist</code></pre><p>grep常用的options：-v,-l, -n</p><p>-v:输出不匹配pattern的内容(reverse)</p><p>-l:查找包含匹配pattern的文件时，只输出文件名，而不输出相应的文件内容。【看例子吧，表达的不是很清楚】</p><p>-n:输出结果中，将匹配到的行数也一并输出</p><p>例子：</p><pre><code class="shell">cat students# 输出张三李四王五赵六# 输出不匹配张三的其他内容grep -v &#39;张三&#39; students# 输出李四王五赵六#  输出以s开头的文件中，包含张三的文件名grep -l &#39;张三&#39; s*# 输出students# 输出中带有张三所在的行数grep -n &#39;张三&#39; students# 输出1:张三</code></pre></li><li><p>sort</p><p>排序，顾名思义。</p><p>例子：</p><pre><code class="shell">sort students</code></pre><p>一些常用的option: -u, -r,-o,</p><p>-u: 表示uniq, 所以 <code>sort -u</code> = <code>sort | uniq</code></p><p>-r: 表示reverse，降序排列。</p><p>-o:将排序后的结果输出到指定的文件中</p><p>-k2n:当存在多列时，指定跳过第一列，以第二列来排序，-k10n，则表示以第10列来排序</p><p>例子：</p><pre><code class="shell">cat numbers# 输出22Charlie12Emanuel43Fred90Lucy33Ralph44Tony44Tonysort numbers# 输出12Emanuel22Charlie33Ralph43Fred44Tony44Tony90Lucysort -u numbers# 输出12Emanuel22Charlie33Ralph43Fred44Tony90Lucysort -ur numbers# 输出90Lucy44Tony43Fred33Ralph22Charlie12Emanuelcat food# 输出pizza    4    goodchesses    5    not so badcoco    10    great!chips    99    yeah!sweets    9    I like them!sort -k2n food# 输出pizza    4    goodchesses    5    not so badsweets    9    I like them!coco    10    great!chips    99    yeah!</code></pre></li><li><p>uniq</p><p>去重。</p><p>基本样式：</p><pre><code class="shell">uniq in_file out_file</code></pre><p>例子：</p><pre><code class="shell">cat numbers# 输出22Charlie12Emanuel43Fred90Lucy33Ralph44Tony44Tonyuniq numbers# 输出22Charlie12Emanuel43Fred90Lucy33Ralph44Tony</code></pre><p>常用的option：-d, -c</p><p>-d: 列出重复的行</p><p>-c: 列出每一行出现的次数</p><p>例子：</p><pre><code class="shell">uniq -d numbers# 输出44Tonyuniq -c numbers# 输出1 22Charlie1 12Emanuel1 43Fred1 90Lucy1 33Ralph2 44Tony</code></pre></li></ul><h4 id="正则"><a href="#正则" class="headerlink" title="正则"></a>正则</h4><pre><code>* :匹配0或者多个字符.*:匹配0或者多个任意字符? :匹配单个字符[0-9] :匹配0-9的单个字符[!a] :匹配除a以外的单个字符^:行首第一个字符$:行尾最后一个字符\{min, max\}: 匹配数量，比如/[a-z]\{4,6\}/，匹配4到6个小写字母组成的字符串,或者/[a-z]\{4\}/，匹配4个小写字母组成的字符串\(...\): 保存匹配项，并将结果依次保存在register1, register 2...比如/^\(.\).*\1$/，表示匹配所有第一个和最后一个字符相同的行</code></pre><h4 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h4><ul><li>变量名由字母，数字，下划线构成</li><li>变量赋值时，<code>=</code>前后不可有空格，未赋值时，默认是null</li><li>shell没有data type的概念，故变量没有类型，一律作为字符串处理</li></ul><p>引用变量时，使用$variable，注意，<strong>shell performs variable substitution before it executes commands.</strong></p><pre><code class="shell">number=99echo There are $number cups of tea on the table.</code></pre><p>内置的整型计算（integer Arithmetic）：</p><pre><code>$((express))</code></pre><p>这里express只能包含数字，算术操作符和变量。</p><p>与之相似的有 <code>expr</code> command。</p><p>例子：</p><pre><code class="shell">a=1echo $a# 输出1: $(( a = a + 1)) # : 表示空命令，执行$((a = a + 1))，但不输出结果echo $a# 输出2echo $(( 100 * 2 ))# 输出200echo $(( a &lt; 1 )) # false 为 0，true 为 1# 输出0echo $(expr $a + 1)# 输出2echo $(expr 9 + 9)# 输出18echo $(expr 9 \* 9)  ## 注意*用了反斜杠\, 不然shell会将*解析成当前目录下的文件列表，造成语法错误。# 输出81</code></pre><p>不过expr 更常用的操作是<code>expr expr1 : expr2</code>,  其中，expr2 是一个正则表达式，该操作用于查找expr1中匹配expr2模式的字符数量。</p><p>例子：</p><pre><code class="shell">expr &quot;hello&quot; : &quot;.*&quot;# 输出5expr &quot;hello&quot; : &quot;[A-Z]*&quot;# 输出0</code></pre><h4 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h4><p>每当执行一个shell程序时，shell会自动将传入的参数依次存储在特殊的变量\$1, \$2,  \$3…..中，这些特殊的变量又称为positional Params。如果需要引用的参数是第10，11….,需要使用<code>${n}</code>，比如<code>${11}</code></p><p>其他一些与参数有关的变量：</p><p>\$<em>: 所有参数列表。如果是”\$\</em>“ , shell 会将”\$*“ 替换成\$1, \$2 ……..</p><p>\$@： 同 \$*, 唯一的区别是，shell 会将”\$@” 替换成”\$1”, “\$2” …….</p><p>\$#: 参数的个数 </p><p>看例子：</p><p>现有shell文件<code>args.sh</code>包含以下内容：</p><pre><code class="shell">#!/usr/bin/env shecho $# arguments passedecho the first argument is $1# $@ vs $*for arg in &quot;$@&quot;do    echo $argdonefor arg in &quot;$*&quot;do    echo $argdone</code></pre><p>执行该文件：</p><pre><code class="shell">sh args.sh a b c d e f# 输出6 arguments passedthe first argument is aabcdefa b c d e f</code></pre><p>这里，”\$*” 循环时，将所有的参数一起输出了。</p><p>如果for循环中的”\$<em>“， 替换成\$\</em>， 则输出结果同\$@。</p><p>参数操作中，可能会涉及到shift命令，用于从左边移除positional parameters， 队列出栈。\$2 的值被赋给了\$1，以此类推。</p><p>修改上面的<code>args.sh</code>文件：</p><pre><code class="shell">#!/bin/bashecho $# arguments passedecho the first argument is $1shiftecho $# arguments passedecho the first argument is $1shiftecho $# arguments passedecho the first argument is $1shiftecho $# arguments passedecho the first argument is $1shift</code></pre><p>执行：</p><pre><code class="shell">sh args.sh a b c d# 输出4 arguments passedthe first argument is a3 arguments passedthe first argument is b2 arguments passedthe first argument is c1 arguments passedthe first argument is d</code></pre><p>参数其实也是变量，参数的引用，赋值和替换还有一些特殊用法，来看看：</p><p>\${params}:  直接返回params的值</p><p>\${params:?value}： 如果params不是null，返回params，否则将value写入标准错误，然后退出程序，如果value为空，则直接输出标准错误信息。</p><p>\${params:-value}：如果params不是null，返回params，否则返回value</p><p>\${params:=value}：同{params:-value}，唯一的区别是value会被赋值给params</p><p>\${params:+value}：\${params:-value}的反面，如果params不是null，返回value，否则返回params(null)，也就是substitute nothing.</p><p>此外，还有一些特殊点的：</p><pre><code>$0: 当前执行脚本的文件名$!: 最后运行的命令的后台PID${#variable}: 返回variable的值的长度，如果variable是数组，返回数组的长度，若是字符串则返回所含字符个数。</code></pre><h4 id="双引号单引号etc"><a href="#双引号单引号etc" class="headerlink" title="双引号单引号etc"></a>双引号单引号etc</h4><p>单引号: 出现在单引号内的所有的特殊字符都会被忽略</p><p>双引号: 除\$ , \, `` 之外的所有特殊字符都会被忽略。</p><p>反斜杠：紧随其后的字符不会被解析</p><p>命令替换 command substitution: ``，等同于<code>$()</code></p><p>看例子，一目了然：</p><pre><code class="shell">x=5echo &#39;$x&#39;# 输出$xecho &quot;$x&quot;# 输出5echo \$x# 输出$xeval echo \$x   # eval: scans twice# 输出5echo date# 输出dateecho `date`# 输出Thu Sep 5 18:37:43 CST 2019echo $(date)# 输出Thu Sep 5 18:37:43 CST 2019filecount=$(ls | wc -l | sed &#39;s/ //g&#39;)echo $filecount# 输出28</code></pre><h4 id="判断"><a href="#判断" class="headerlink" title="判断"></a>判断</h4><p>主要涉及if 和 case的用法。</p><p>if的一般模式：</p><pre><code class="shell">if command1then    command2    ......else    command3    ......fi</code></pre><p>在使用if 前，先看如何获取exit status 以及判断条件时所用的test 命令。</p><p>通过 \$? 可以获取最后一次命令的exit status，运行脚本时，\$?默认返回最后一次命令的状态。但你也可以直接使用 <code>exit n</code> 来退出当前程序 ，这里 n 是 0～255之间的整数，0表示程序成功。</p><p>而test命令是shell 的一个内置命令，基本模式 :</p><pre><code class="shell">test expression</code></pre><p>其中 expression 表示需要测试的条件。</p><p>比如：</p><pre><code class="shell">x=1test $x = 1echo $?# 输出0</code></pre><p>这里最好是给\$x 包上””，如果x没有被赋值，<code>test $x = 1</code>会出现语法错误。「因为shell会替换\$x 为 null，然后传给test 的只有两个参数：= 和1，导致语法错误。」</p><p>看个例子感受下：</p><pre><code class="shell">name=test $name = &#39;Ruby&#39;# 输出parse error: condition expected: =blanks=&quot;  &quot;test -z &quot;$blanks&quot; # 判断是否是null，不是则返回true(0)，否则返回false(1),  -z: is null,length zeroecho $?# 输出1test -n &quot;$blanks&quot; # 判断是否不是null，不是则返回true(0)，否则返回false(1), -n: not nullecho $?# 输出0</code></pre><p>test 的另一种样式长这样：</p><pre><code>[ expression ]</code></pre><p> 所以 if  后面出现的那些，其实都是test 命令，是不是有种顿悟的错觉？</p><p>这里注意[[]] 和[] 的区别，在流程控制中， [[]] 中允许直接使用 || ，&amp;&amp; 等逻辑符号。但在 [] 可以使用 -a(and)，-o(or)</p><p>举个例子：</p><pre><code class="shell">#!/bin/bashx=2if [ &quot;$x&quot; -ge 2 -a &quot;$x&quot; -le 3 ] then    echo &quot;x is between 2 and 3.&quot;fiif [[ &quot;$x&quot; -ge 2 &amp;&amp; &quot;$x&quot; -le 3 ]]then    echo &quot;x is between 2 and 3.&quot;fi</code></pre><p>但一般不推荐在[]中使用-a, -o，多使用 [ p ] &amp;&amp; [ q ] 或者 [ p ] || [ q ] 。</p><p>看一个判断文件的例子：</p><pre><code class="shell">#!/bin/bashfile1=&quot;./test_file1&quot;if [ -e &quot;$file1&quot; ]; then    echo &quot;$file1 exists&quot;elif [ -f &quot;$file1&quot; ]; then    echo &quot;$file1 is a normal file&quot;elif [ -r &quot;$file1&quot; ]; then    echo &quot;$file1 is readable&quot;elif [ -w &quot;$file1&quot; ]; then    echo &quot;$file1 is writable&quot;elif [ -x &quot;$file1&quot; ]; then    echo &quot;$file1 is executable&quot;elif [ -d &quot;$file1&quot; ]; then    echo &quot;$file1 is a directory&quot;elif [ -L &quot;$file1&quot; ]; then    echo &quot;$file1 is a symbolic link&quot;elif [ -p &quot;$file1&quot; ]; then    echo &quot;$file1 is a named pipe&quot;elif [ -S &quot;$file1&quot; ]; then    echo &quot;$file1 is a network socket&quot;elif [ -G &quot;$file1&quot; ]; then    echo &quot;$file1 is owned by the group&quot;elif [ -O &quot;$file1&quot; ]; then    echo &quot;$file1 is owned by the userid&quot;fi</code></pre><p>再来看case。</p><p>一般模式：</p><pre><code class="shell">case value inpattern1)    command1                    ......                    command;;pattern2)    command2                    ......                    command;;.......patternn)    commandn                    ......                    command;;esac</code></pre><p>这里pattern用的是正则的东西。</p><p>pattern也可以用 | 来表示逻辑或，比如pat1 | pat2 | …..|pat-n，表示匹配 pat1,pat2, ….pat-n 中任一个即可。</p><p>看个例子：</p><pre><code class="shell">cat greetings# 输出#!/bin/bashhour=$(date +%H)case &quot;$hour&quot;in    0? | 1[01]) echo &quot;Good morning&quot;;;    1[2-7]) echo &quot;Good afternoon&quot;;;    *) echo &quot;Good evening&quot;;;esacsh greetings# 输出Good afternoon</code></pre><p>另外需要注意两个特别的constructs:</p><p>Command1 &amp;&amp; command2: 只有当command1返回的exit status是0(success)，才会执行command2。</p><p>Command1 || command2: 只有当command1返回的exit status是非0(fail)，才会执行command2。</p><h4 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h4><p>主要是for, while, until. </p><ul><li><p>for</p><p>基本模式：</p><pre><code class="shell">for var in word1, word2...wordndo    command1    command2    .......done</code></pre><p>这里举一个有些特殊的用法。for可以省略后面的<code>in word1, word2...wordn</code>:</p><pre><code class="shell">for vardo    command1    command2    .......done</code></pre><p>shell会自动遍历所有的参数。上面的代码等价于:</p><pre><code class="shell">for var in &quot;$@&quot;do    command1    command2    .......done</code></pre><p>例子：</p><pre><code class="shell">cat for_example# 输出#!/bin/bashfor argdo    echo $argdonesh for_example 1 2 3# 输出123</code></pre></li><li><p>while</p><p>一般模式：</p><pre><code class="shell">while commandtdo    command1    command2    .......done</code></pre><p>例子：</p><pre><code class="shell">cat while_example# 输出#!/bin/bashwhile [ &quot;$#&quot; -ne 0 ]do    echo &quot;$1&quot;    shiftdonesh while_example 1 2 3# 输出123</code></pre></li><li><p>until</p><p>一般模式：</p><pre><code class="shell">until commandtdo    command1    command2    .......done</code></pre><p>例子：</p><pre><code class="shell">cat until_example# 输出#!/bin/bashuntil [ &quot;$#&quot; -eq 0 ]do    echo &quot;$1&quot;    shiftdonesh until_example 1 2 3# 输出123</code></pre></li><li><p>break跳出loop</p><p><code>break</code>: 退出当前loop</p><p><code>break n</code>：跳出的循环层级，比如:</p><pre><code class="shell">for filedo    while [ &quot;$x&quot; -gt 1 ]    do        .....        if [ -n &quot;$error&quot;]        then            break 2        fi        ....    done  ......done</code></pre><p>如果error 不为null, for 和while 循环都会退出。</p></li><li><p>continue跳过</p><p>等同于ruby里面的next. 「从起名字的角度看，next比continue要好， next 更能表达出当前iteration后面的命令不再执行的含义，咱直接去下一个iteration。」</p></li><li><p>loop后台执行</p><p>同命令后台执行一样，在loop 的结尾添加 &amp; 即可。</p><pre><code class="shell">for file in memo[1-4]do    run $filedone &amp;</code></pre></li><li><p>getopts</p><p>shell内置的command，用来处理命令行参数，后面另起一篇单独说它，这里略过。</p></li></ul><h4 id="read-和-printf"><a href="#read-和-printf" class="headerlink" title="read 和 printf"></a>read 和 printf</h4><ul><li><p>read</p><p>从终端或者文件中读取数据。</p><p>read 命令的exit status 只有当遇到 end-of-file 时才会为非0(fail)。具体而言，如果是从终端读取数据，则当用户按下ctrl+d 时，如果是从文件读取，则是当文件中已经没有数据的时候。</p><p>例子：</p><pre><code class="shell">cat addi# 输出#!bin/bashwhile read n1 n2do    echo $(( $n1 + $n2 ))donesh addi111 222# 输出333# 使用ctrl + d 退出</code></pre></li><li><p>printf</p><p>格式化输出。</p><p>一般模式：</p><pre><code class="shell">printf &quot;format&quot; arg1 arg2 ....</code></pre><p>这块其实蛮复杂的，简单举几个例子：</p><pre><code class="shell">printf &quot;This is a number: %d\n&quot; 10# 输出This is a number: 10printf &quot;The octal value for %d is %o\n&quot; 10 10# 输出The octal value for 10 is 12printf &quot;The hexadecimal value for %d is %x\n&quot; 10 10# 输出The hexadecimal value for 10 is aprintf &quot;A string: %s and a character: %c\n&quot; hello A# 输出A string: hello and a character: Aprintf &quot;Just the first character: %c\n&quot; hello# 输出Just the first character: hprintf &quot;%.5d %.4X\n&quot; 10 27# 输出00010 001B</code></pre></li></ul><h4 id="exec-与eval"><a href="#exec-与eval" class="headerlink" title="exec 与eval"></a>exec 与eval</h4><p>想起ruby里面的instance_exec, instance_eval……</p><p>单独拎出来，是觉得这俩有些傻傻分不清。</p><p>eval:  前面的例子中其实有提到它，shell 会在执行前对命令行会多看一眼「可能因为这个命令行长得好看😄」。比如：</p><pre><code class="shell">x=1echo \$x# 输出$xeval echo \$x# 输出1</code></pre><p>exec: 用于替换当前的程序，一般模式<code>exec program</code>，当前的进程会被改变，用该program来替换当前的进程。exec还可以用来重定向标准输入输出。</p><h4 id="有关debug"><a href="#有关debug" class="headerlink" title="有关debug"></a>有关debug</h4><p>建议在脚本中直接使用<code>set -euxo pipefail</code> 。</p><p>set的常用的几个option:</p><blockquote><p>-x : 开启追踪，类似脚本debug时，添加了+x</p><p>-u: 用于处理那些未定义变量。Treat unset variables and parameters other than the special parameters ‘@’ or ‘*’ as an error when performing parameter expansion. </p><p>-e: 只要有错就退出程序，终止执行，返回非0值。Exit immediately if a pipeline, which may consist of a single simple command , a list, or a compound command  returns a non-zero status. </p></blockquote><p>但是set -e 不适用管道命令，解决方法是：set -eo pipefail</p><p>综上，完整的处理出错的脚本：</p><pre><code class="shell">set -euxo pipefail</code></pre><p>set除了在debug中使用外，还可用于给positional params重新赋值，而它的兄弟unset，用于移除变量在当前环境下的定义(不过unset 对<strong>readonly</strong>的变量无效)：</p><pre><code class="shell">set a b c                         # 分别将a,b,c 赋给 $1, $2, $3echo $1:$2:$3x=hiecho $x# 输出hiunset xecho $x# 输出</code></pre><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://www.amazon.com/Shell-Programming-Unix-Linux-Developers-ebook/dp/B01L480ILM" target="_blank" rel="noopener">《Shell Programming in Unix, Linux and OS X》forth edition</a></p><p><a href="http://tldp.org/LDP/abs/html/index.html" target="_blank" rel="noopener">Advanced Bash-Scripting Guide</a></p><p><a href="https://www.youtube.com/watch?v=hwrnmQumtPw&amp;list=PLwidFWzbt81iYkUtN1MXxb7htI48h_MMp&amp;index=3&amp;t=0s" target="_blank" rel="noopener">Shell Scripting Tutorial</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;算是&lt;a href=&quot;https://www.amazon.com/Shell-Programming-Unix-Linux-Developers-ebook/dp/B01L480ILM&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;《Shell Programming in Unix, Linux and OS X》forth edition&lt;/a&gt;的读书笔记了。&lt;/p&gt;
&lt;p&gt;如果想要更快入坑，强烈推荐Derek Banas 的1小时视频 &lt;a href=&quot;https://www.youtube.com/watch?v=hwrnmQumtPw&amp;amp;list=PLwidFWzbt81iYkUtN1MXxb7htI48h_MMp&amp;amp;index=3&amp;amp;t=0s&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Shell Scripting Tutorial&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Shell" scheme="http://lupeipei.github.io/tags/Shell/"/>
    
  </entry>
  
  <entry>
    <title>清除PG table column 中的 non break space</title>
    <link href="http://lupeipei.github.io/2019/08/28/replace-no-break-space-in-postgresql/"/>
    <id>http://lupeipei.github.io/2019/08/28/replace-no-break-space-in-postgresql/</id>
    <published>2019-08-28T02:20:36.000Z</published>
    <updated>2019-08-28T02:26:20.573Z</updated>
    
    <content type="html"><![CDATA[<p>清除PG cell中的 non break space.<br><a id="more"></a></p><h4 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h4><p>Postgresql 10.4.</p><p>现有 table posters， 含 column content_html， content_html 中可能包含类似<code>&lt;td&gt;&amp;nbsp;&lt;/td&gt;</code>的内容。需要移除该column中的出现的non-break space，但该column 关联其他的attribute，不能直接使用replace来修改，需要先找到对应的poster记录，然后一并修改。</p><p>尝试查找对应记录：</p><pre><code class="ruby">Poster.where(&quot;content_html LIKE ?&quot;, &#39;%&amp;nbsp;%&#39;)</code></pre><p>无效。</p><h4 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h4><p>Google后尝试：</p><pre><code class="ruby">Poster.where(&quot;content_html LIKE ?&quot;, &quot;%\xc2\xa0%&quot;)</code></pre><p>看stack overflow上<a href="https://stackoverflow.com/questions/36849886/replacing-nonbreaking-spaces-a0-in-postgres" target="_blank" rel="noopener">Replacing nonbreaking spaces (%A0) in Postgres</a>，有人说该方法无效，可能是PG版本问题？PG 10.4是可以的。</p><p>或者改用Unicode characters：</p><pre><code class="ruby">Poster.where(&quot;content_html LIKE ? OR content_html LIKE ?&quot;, &quot;%\u00a0%&quot;, &quot;%\u2002%&quot;)</code></pre><p>更新时，使用:</p><pre><code class="ruby">poster.content_html.tr(&quot;%\xc2\xa0%&quot;, &quot; &quot;)# orposter.content_html.tr(&quot;%\u00a0%&quot;, &quot; &quot;)poster.content_html.tr(&quot;%\u2002%&quot;, &quot; &quot;)</code></pre><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://stackoverflow.com/questions/36849886/replacing-nonbreaking-spaces-a0-in-postgres" target="_blank" rel="noopener">Replacing nonbreaking spaces (%A0) in Postgres</a></p><p><a href="[http://www.fileformat.info/info/unicode/category/Zs/list.htm](http://www.fileformat.info/info/unicode/category/Zs/list.htm">Unicode Characters in the ‘Separator, Space’ Category</a>)</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;清除PG cell中的 non break space.&lt;br&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Rails" scheme="http://lupeipei.github.io/tags/Rails/"/>
    
      <category term="PostgreSQL" scheme="http://lupeipei.github.io/tags/PostgreSQL/"/>
    
  </entry>
  
  <entry>
    <title>shell笔记之sed,tr,cut,paste</title>
    <link href="http://lupeipei.github.io/2019/08/10/shell-notes-of-sed-tr-cut-paste/"/>
    <id>http://lupeipei.github.io/2019/08/10/shell-notes-of-sed-tr-cut-paste/</id>
    <published>2019-08-10T11:55:55.000Z</published>
    <updated>2019-08-10T11:58:00.701Z</updated>
    
    <content type="html"><![CDATA[<p>sed，tr, cut, paste 的扫盲笔记。</p><a id="more"></a><h4 id="sed"><a href="#sed" class="headerlink" title="sed"></a>sed</h4><p>sed: stream editor .</p><h5 id="用途"><a href="#用途" class="headerlink" title="用途"></a>用途</h5><p>处理流的神器。</p><h5 id="format"><a href="#format" class="headerlink" title="format"></a>format</h5><pre><code>sed command file</code></pre><p>这里的 command 是 ed-style 的command。「ed: Ken Thompson发明，算是 vi 的前身。详情戳 <a href="https://en.wikipedia.org/wiki/Ed_(text_editor" target="_blank" rel="noopener">ed wiki</a>)」</p><h5 id="examples"><a href="#examples" class="headerlink" title="examples"></a>examples</h5><pre><code class="shell">$ cat fruitsapples I have two apples.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.watermelon I like watermelon.$ sed &#39;s/like/have/&#39; fruits   # 将每一行出现的第一个 like 替换成 haveapples I have two apples.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.watermelon I have watermelon.$ sed &#39;s/apples/lemons/g&#39; fruits  # g stands for global, 全局替换 apples 为 lemonslemons I have two lemons.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.watermelon I like watermelon.</code></pre><p>使用sed<strong>并不会改变文件原来的内容</strong>，如果想要将标准输出用于原文件，可以这样来：</p><pre><code class="shell">$ cat fruitsapples I have two apples.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.watermelon I like watermelon.$ sed &#39;s/like/have/&#39; fruits &gt; temp$ mv temp fruits$ cat fruitsapples I have two apples.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.watermelon I have watermelon.</code></pre><h5 id="n-option"><a href="#n-option" class="headerlink" title="-n option"></a>-n option</h5><p>sed默认输出所有的行，可以使用 -n 来指定输出的行。</p><p>例：</p><pre><code class="shell">$ sed -n &#39;1,2p&#39; fruits                            # 输出第1、2行apples I have two apples.bananas I have three bananas$ sed -n &#39;1,$p&#39; fruits                            # 输出第1到最后一行，即所有行apples I have two apples.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.watermelon I have watermelon.$ sed -n &#39;/pear/p&#39; fruits                     # 输出包含 pear 的行pear I have no pear.$ sed -n l fruits       # n 后面跟的是字母l,输出所有行，同时将分隔符转成\xx[octal value]一并打印出来apples I have two apples.$bananas I have three bananas.$cherries I have lots of cherries.$cucumber I have a cucumber.$pear I have no pear.$watermelon I have watermelon.$</code></pre><p>常见的字符及对应的octal value可以参看这里 <a href="http://www.asciitable.com/" target="_blank" rel="noopener">AsciiTable</a></p><h5 id="delete-lines"><a href="#delete-lines" class="headerlink" title="delete lines"></a>delete lines</h5><p>使用 d 可以删除行，这里d不是option，而是command。「当然，使用d并没有真的删除原文件，只是输出中删除了指定的行」</p><p>例：</p><pre><code class="shell">$ sed &#39;2,3d&#39; fruits                            # 删除第2，3行apples I have two apples.cucumber I have a cucumber.pear I have no pear.watermelon I have watermelon.$ sed &#39;/watermelon/d&#39; fruits    # 删除含有 watermelon 的行apples I have two apples.bananas I have three bananas.cherries I have lots of cherries.cucumber I have a cucumber.pear I have no pear.</code></pre><h4 id="tr"><a href="#tr" class="headerlink" title="tr"></a>tr</h4><h5 id="用途-1"><a href="#用途-1" class="headerlink" title="用途"></a>用途</h5><p>用于转换或删除(-d option），比较常用的是大小写转换。</p><h5 id="format-1"><a href="#format-1" class="headerlink" title="format"></a>format</h5><pre><code>tr from-chars to-chars</code></pre><p>一些特殊字符，比如tab，可以使用对应的octal value来代替。</p><h5 id="examples-1"><a href="#examples-1" class="headerlink" title="examples"></a>examples</h5><p>我们把fruits 文件内容都换成大写：</p><pre><code class="shell">$ tr &#39;[a-z]&#39; &#39;[A-Z]&#39; &lt; fruitsAPPLES I HAVE TWO APPLES.BANANAS I HAVE THREE BANANAS.CHERRIES I HAVE LOTS OF CHERRIES.CUCUMBER I HAVE A CUCUMBER.PEAR I HAVE NO PEAR.WATERMELON I HAVE WATERMELON.$ tr &#39;[a-z]&#39; &#39;[A-Z]&#39; &lt; fruits | tr &#39;[A-Z]&#39; &#39;[a-z]&#39;apples i have two apples.bananas i have three bananas.cherries i have lots of cherries.cucumber i have a cucumber.pear i have no pear.watermelon i have watermelon.$ date | tr &#39;&#39; &#39;\12&#39;  # 将空白符转换为换行符SatAug1018:40:01CST2019</code></pre><h5 id="s-option"><a href="#s-option" class="headerlink" title="-s option"></a>-s option</h5><p>s stands for squeeze， 挤，榨。意思是转换后，to-chars里面连续出现的多个都会被”挤”成一个char。</p><p>比较常见的是移除不必要的空格。</p><p>例：</p><pre><code class="shell">$ cat lotsspacesso    you   just   getthis    one!Open         the    door, please!$ tr -s &#39; &#39; &#39; &#39; &lt;  lotsspacesso you just getthis one!Open the door, please!</code></pre><h5 id="d-option"><a href="#d-option" class="headerlink" title="-d option"></a>-d option</h5><p>使用 -d, tr 可以用来删除输入流中指定的字符。 </p><p>format长这样：</p><pre><code>tr -d from-chars</code></pre><p>例：</p><pre><code class="shell">$ cat lotsspacesso    you   just   getthis    one!Open         the    door, please!$ tr -d &#39; &#39; &lt; lotsspaces   # 删除空白符soyoujustgetthisone!Openthedoor,please!</code></pre><p>这里需要记住，tr 仅可以作用于单个字符（single character) ，如果需要转换多个字符，比如将 apples 换成 lemons，建议使用sed.</p><h4 id="cut"><a href="#cut" class="headerlink" title="cut"></a>cut</h4><h5 id="用途-2"><a href="#用途-2" class="headerlink" title="用途"></a>用途</h5><p>从文件或者标准输出中提取指定的区域 (extract various fields of data from file or the output of a command)。</p><h5 id="format-2"><a href="#format-2" class="headerlink" title="format"></a>format</h5><pre><code>cut -cchars file</code></pre><h5 id="examples-2"><a href="#examples-2" class="headerlink" title="examples"></a>examples</h5><pre><code class="shell">$ cat foodpizza    4    goodchesses    5    not so badcoco    10    great!chips    99    yeah!sweets    9    I like them!$ cat -c1-6 food                 #提取前6个字符pizzachessecoco    1chipssweets$ cut -c1-6,10- food    #提取前6个字符 和 第10个以后的所有字符pizza    oodchesse    not so badcoco    1reat!chips    yeah!sweetsI like them!</code></pre><h5 id="d-f-options"><a href="#d-f-options" class="headerlink" title="-d -f options"></a>-d -f options</h5><blockquote><p>-d: 指定界定符(specifying the field separator delimiter)</p><p>-f:指定想要抽取出的区域(the field you want to extract)</p></blockquote><p>看个例子就懂了。</p><pre><code class="shell">$ cat users                                 # 从 /etc/passwd 里面切出来的部分内容nobody:*:-2:-2:Unprivileged User:/var/empty:/usr/bin/falseroot:*:0:0:System Administrator:/var/root:/bin/shdaemon:*:1:1:System Services:/var/root:/usr/bin/false_uucp:*:4:4:Unix to Unix Copy Protocol:/var/spool/uucp:/usr/sbin/uucico_taskgated:*:13:13:Task Gate Daemon:/var/empty:/usr/bin/false_networkd:*:24:24:Network Services:/var/networkd:/usr/bin/false_installassistant:*:25:25:Install Assistant:/var/empty:/usr/bin/false$ cut -d: -f1 users                # 可以获得用户名nobodyrootdaemon_uucp_taskgated_networkd_installassistant</code></pre><p>-d 可以后跟其他的分隔符，但是一些特殊的分隔符，比如tab, 就会抛错：bad delimiter, 这种情况下，可以考虑改用sed。</p><h4 id="paste"><a href="#paste" class="headerlink" title="paste"></a>paste</h4><h5 id="用途-3"><a href="#用途-3" class="headerlink" title="用途"></a>用途</h5><p>与cut 刚好相反，paste 用来连接多个区域。</p><h5 id="format-3"><a href="#format-3" class="headerlink" title="format"></a>format</h5><pre><code>paste files</code></pre><h5 id="examples-3"><a href="#examples-3" class="headerlink" title="examples"></a>examples</h5><pre><code class="shell">$ cat students张三李四王五赵六$ cat scores899010089$ paste students scores             # 默认用 Tab 分隔，可以用sed -n l 查看分隔符张三    89李四    90王五    100赵六    89$ paste students scores | sed -n l # 分隔符为 \t张三\t89$李四\t90$王五\t100$赵六\t89$</code></pre><h5 id="d-option-1"><a href="#d-option-1" class="headerlink" title="-d option"></a>-d option</h5><p>同 cut, -d用于指定 分隔符，默认为tab。 继续上面的例子：</p><pre><code class="shell">$ paste -d&#39;:&#39; students scores张三:89李四:90王五:100赵六:89$ paste -d&#39;&gt;&#39; students scores张三&gt;89李四&gt;90王五&gt;100赵六&gt;89</code></pre><p>注意 -d指定的分隔符只能是单个，如果指定了多个，会默认取第一个。</p><pre><code class="shell">$ paste -d&#39;&gt;++&gt;&gt;&#39; students scores张三&gt;89李四&gt;90王五&gt;100赵六&gt;89</code></pre><p>为了安全性，建议分隔符用单引号包起来。不信试试下面这个 : P</p><pre><code class="shell">$ paste -d&gt; students scores$ cat students</code></pre><h5 id="s-option-1"><a href="#s-option-1" class="headerlink" title="-s option"></a>-s option</h5><p>将给定文件的每一行连接起来。(paste lines together)</p><p>看例子：</p><pre><code class="shell">$ paste -s students                  # 默认分隔符为 tab张三    李四    王五    赵六$ paste -d&#39;;&#39; -s students  # 指定分隔符为 ;张三;李四;王五;赵六$ ls | paste -d&#39;;&#39; -s -   # 当输入来自标准输出时，需要用 -food;scores;students;users</code></pre><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://en.wikipedia.org/wiki/Ed_(text_editor" target="_blank" rel="noopener">ed wiki</a>)</p><p> <a href="http://www.asciitable.com/" target="_blank" rel="noopener">AsciiTable</a></p><p><a href="https://www.amazon.com/Shell-Programming-Unix-Linux-Developers-ebook/dp/B01L480ILM" target="_blank" rel="noopener">shell programming in Unix Linux and OSX</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;sed，tr, cut, paste 的扫盲笔记。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Shell" scheme="http://lupeipei.github.io/tags/Shell/"/>
    
  </entry>
  
  <entry>
    <title>读《怦然心动的人生整理魔法》</title>
    <link href="http://lupeipei.github.io/2019/07/16/notes-of-the-life-changing-magic-of-tiding-up/"/>
    <id>http://lupeipei.github.io/2019/07/16/notes-of-the-life-changing-magic-of-tiding-up/</id>
    <published>2019-07-16T10:08:51.000Z</published>
    <updated>2019-07-16T10:11:23.648Z</updated>
    
    <content type="html"><![CDATA[<p>谢谢你们曾让我心动。</p><a id="more"></a> <h4 id="全书脉络"><a href="#全书脉络" class="headerlink" title="全书脉络"></a>全书脉络</h4><p>全书5章，整本书的脉络：</p><ul><li><p>第一章列出过往的整理中常见的错误观念，引导读者建立正确的整理观念；</p></li><li><p>第二章以丢弃物品为中心，给出决定物品去留的标准以及丢弃过程中可能遇到的问题；</p></li><li><p>第三章详细讲述如何对衣服，书籍，文件，纪念品等物品进行正确的归类整理；</p></li><li><p>第四章给出了各种收纳建议；</p></li><li><p>第五章列举整理对人生产生的诸多戏剧性影响。</p></li></ul><p>要点部分：</p><p>整理的诀窍：<strong>一次性，短期内，彻底整理</strong>。【这里的短期，以作者的标准，最长为半年。】</p><p>采用正确的方法来整理，关键点只有两个：</p><ul><li>确定物品的弃留</li><li>设定物品放置的位置</li></ul><p>且<strong>两者的顺序不可更改</strong>，必须先完成物品的丢弃工作，才能进行后续的整理收纳。</p><p>决定物品弃留的标准只有一个：<strong>触碰时，是否让你怦然心动</strong>。</p><p>收纳整理的关键只有一个：简化到极限。「极限的标准因人而异」</p><p>具体实操时，作者给出了很多的建议，篇幅较多，不一一列举。</p><p>这里只记录下个人觉得蛮实用的一些建议。</p><h5 id="丢弃物品时的几个tips："><a href="#丢弃物品时的几个tips：" class="headerlink" title="丢弃物品时的几个tips："></a>丢弃物品时的几个tips：</h5><ul><li>把同类物品集中起来，进行一次性判断；</li><li>别让家人看见你丢弃的物品；</li><li>面对不会整理的家人，最有效的方法就是先默默丢弃自己的物品，家人会潜移默化受影响；</li><li>对于那些“没有心动感也不愿丢弃”的物品，思考它们真正的功能，对其表示谢意然后放手；</li><li>除非你产生”只要拥有这些物品，就能过上幸福生活”的感觉，否则，请继续丢弃，直到适量的感悟点到来。</li></ul><h5 id="物品分类整理的几个tips："><a href="#物品分类整理的几个tips：" class="headerlink" title="物品分类整理的几个tips："></a>物品分类整理的几个tips：</h5><ul><li><p>不要因为”扔了可惜”，把衣服降格成家居服；</p></li><li><p>竖着折叠衣服，袜子；</p></li><li><p>衣柜左边放深色材质厚的衣物，右边则放浅色材质薄的衣物。</p></li></ul><h5 id="收纳的几个tips："><a href="#收纳的几个tips：" class="headerlink" title="收纳的几个tips："></a>收纳的几个tips：</h5><ul><li>一个不漏的设定物品的固定位置；</li><li>简化到极限，使自己拥有的物品数量始终处于可控状态；</li><li>就近收纳同类物品，但不要过度考虑行动路线，避开以方便取物为首要标准的陷阱；</li><li>竖着收纳；</li><li>不要使用市售收纳用具，建议用空鞋盒或者苹果公司的很多产品包装盒；</li><li>浴室和厨房水槽什么都不要放。</li></ul><h4 id="感触联想"><a href="#感触联想" class="headerlink" title="感触联想"></a>感触联想</h4><p>列出个人觉得印象深刻的一些点：</p><ul><li>整理之前，请先思考你所想要的理想生活以及为什么你想要过那样的生活；</li><li>为了珍惜真正有价值的物品，必须首先丢弃已经完成了使命的物品；</li><li>丢弃物品，并不意味着过去的人生体验和自我认同就此消失。通过选用有心动感的物品，更明白自己喜欢什么、追求什么；</li><li><p>拥有的物品准确地诉说着你做过的所有选择。因此，整理的过程也可以说是盘点自己得失优劣的过程；</p></li><li><p>整理不过是手段，整理本身不是目的，真正重要的是整理后如何生活下去。</p></li></ul><p>看第五章的时候，对作者提到的那些因为整理而给人生带来的改变，虽诧异，但细想下也觉得合理。与其说一个人通过整理，让房间长久保持整洁的状态，不如说通过整理，TA跟过往的自己来了一次安静的谈话，一次颇有意义的复盘，在这不断的取舍抉择中，更加清楚自己要的到底是什么。</p><p>刷的过程中，不自觉联想起之前不知道在哪里看到的一篇文章，Google了半天也没找出来源，简单凭记忆说一下吧。</p><p>文章讲述了作者自己一天的工作，其中有一个细节，就是他在乘坐地铁时，会检查当天拿到的票据或者其他文件，作者的处理方式很简单，拍照，上传，然后扔掉原件，删除照片。当时觉得很不理解，怎么能这么干脆利落的扔掉？如果需要的时候找不到这些东西怎么办？现在看来，这哥们应该对整理颇有心得。</p><p>看完整本书后，我并没有立即整理房间，毕竟是个大工程，不过倒是整理了下自己那些杂乱的相册，删掉了很多不必要的图片，也发现了这个：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g51p27cxzoj30d607sdg6.jpg" alt=""></p><p>估计是曾经刷quora时截图的，感觉很配这本书，附上。</p><p>等我也完成自己的整理工作后，再回来打卡吧。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;谢谢你们曾让我心动。&lt;/p&gt;
    
    </summary>
    
      <category term="Reading" scheme="http://lupeipei.github.io/categories/Reading/"/>
    
    
      <category term="Reading" scheme="http://lupeipei.github.io/tags/Reading/"/>
    
  </entry>
  
  <entry>
    <title>Nuxt中低版本浏览器不兼容问题的处理</title>
    <link href="http://lupeipei.github.io/2019/07/12/check-browser-compatible-in-nuxt/"/>
    <id>http://lupeipei.github.io/2019/07/12/check-browser-compatible-in-nuxt/</id>
    <published>2019-07-12T01:47:50.000Z</published>
    <updated>2019-07-12T02:11:17.105Z</updated>
    
    <content type="html"><![CDATA[<p>浏览器版本兼容问题很常见，这里记录下近期踩的坑：在nuxt 项目中，如何处理低版本浏览器的不兼容问题。</p><a id="more"></a><h4 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h4><p>网页在chrome 75 及 Safari 12 中均可以正常显示，但对 360安全浏览器 8.1版本的支持并不友好，360中存在的主要问题是上下滚动条消失，鉴于360 8.1 版本的浏览器使用的内核是 chrome 45，故 下载了chrome 45 测试，发现chrome 45 也存在相同的问题，故问题转变为 如何处理内核 为chrome 45- 的浏览器不兼容问题。</p><h4 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h4><p>最直接的方式：通过 userAgent 判断浏览器类型及版本，然后针对不兼容的低版本浏览器，显示页面，提示用户进行版本升级或者更换其他的浏览器。</p><p>思路没有问题，网上也搜出来了一堆判断浏览器类型及版本的示例代码，但这些处理方式的基本思路是先判断浏览器类型，然后判断版本，而这里需要处理的是 内核 为chrome的浏览器，可能是chrome，可能是360， 也可能是opera，只要版本低于45，则要求升级或更换。故，首先得去解析userAgent。</p><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Headers/User-Agent" target="_blank" rel="noopener">User Agent mozilla </a>  中给出了UA的基本组成 以及 主流浏览器的UA 字符串：</p><p>Web端 User -Agent 基本组成规则：</p><pre><code>Mozilla/&lt;version&gt; (&lt;system-information&gt;) &lt;platform&gt; (&lt;platform-details&gt;) &lt;extensions&gt;</code></pre><p>部分主流浏览器的UA示例：「在 <a href="[http://tools.jb51.net/table/useragent](http://tools.jb51.net/table/useragent">常用浏览器(PC,移动) user-agent</a>) 可以查到 UC，腾讯TT等部分国内浏览器的UA。」</p><ul><li><p>Chrome</p><blockquote><p>Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/51.0.2704.103 Safari/537.36</p></blockquote></li><li><p>Firefox</p><blockquote><p>Mozilla/5.0 (Macintosh; Intel Mac OS X x.y; rv:42.0) Gecko/20100101 Firefox/42.0</p></blockquote></li><li><p>Opera</p><blockquote><p>Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/51.0.2704.106 Safari/537.36 OPR/38.0.2220.41</p></blockquote></li><li><p>Safari</p><blockquote><p>Mozilla/5.0 (iPhone; CPU iPhone OS 10_3_1 like Mac OS X) AppleWebKit/603.1.30 (KHTML, like Gecko) Version/10.0 Mobile/14E304 Safari/602.1</p></blockquote></li><li><p>IE</p><blockquote><p>Mozilla/5.0 (compatible; MSIE 9.0; Windows Phone OS 7.5; Trident/5.0; IEMobile/9.0)</p></blockquote></li></ul><p>根据UA 的基本组成规则，可以考虑解析整个UA，分成三部分：</p><pre><code> Mozilla/&lt;version&gt; (&lt;system-information&gt;) &lt;platform&gt; (&lt;platform-details&gt;) &lt;extensions&gt;</code></pre><p>部分浏览器可能仅有第一部分，比如IE，这样可以根据 system-information 来判断是否为IE，并获取其版本，而针对chrome 部分，在解析得到的extensions 中进行二次解析，先根据空白符分割成数组，再针对数组中的每一个对象，进行split(‘/‘)，取到每一个extension的name 和version，最后只需要判断chrome 是否存在该数组中，存在则取其对应version，判断version是否过低即可。</p><p>具体的解析代码这里就不列出来了。</p><p>另一个需要说明的地方，是让这段处理浏览器不兼容的代码具有可扩展性。</p><p>比如这次处理的是chrome 45 以下版本，下一次如果发现腾讯TT 4.0 以下版本存在不兼容问题，需要在不修改主代码的情况下，保证功能正常。</p><p>鉴于自己写得很烂，这里直接借用领导的处理方式：</p><ul><li><p>新建变量blacklist，将所有不兼容的浏览器，版本及规则放入blacklist中。</p></li><li><p>新建class AgentInfo，在AgentInfo内部定义extension，test等函数。</p></li><li><p>使用parseAgentInfo方法，解析 userAgent，将解析后的结果塞给 AgentInfo ，进行初始化，得到AgentInfo的实例。</p></li><li><p>针对blacklist中的rules，逐一进行测试，如有未通过测试的，则表示该浏览器过低，跳转至新页面，要求用户升级或者更换浏览器。</p></li></ul><p>大致代码如下：</p><pre><code class="javascript">const blacklist = [  { name: &#39;chrome &lt;= 45&#39;, rules: [    (info) =&gt; info.extension(&#39;Chrome&#39;).versionNumber &lt;= 45  ]}]export default function ({ route, req, redirect }) {  const userAgent = process.server ? req.headers[&#39;user-agent&#39;] : navigator.userAgent  const agentInfo = parseAgentInfo(userAgent)  const uncompatible = _.some(blacklist, (item) =&gt; agentInfo.test(item.rules))  if (uncompatible) {    redirect(&#39;/compatible&#39;)  }}</code></pre><p>后面如果需要排除其他不兼容的浏览器，只需更改blacklist即可。</p><p>这部分是中间件的内容，不要忘记在<code>nuxt.config.js</code> 中声明对应的文件名。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Headers/User-Agent" target="_blank" rel="noopener">User Agent mozilla </a><br><a href="[http://tools.jb51.net/table/useragent](http://tools.jb51.net/table/useragent">常用浏览器(PC,移动) user-agent</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;浏览器版本兼容问题很常见，这里记录下近期踩的坑：在nuxt 项目中，如何处理低版本浏览器的不兼容问题。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Nuxt" scheme="http://lupeipei.github.io/tags/Nuxt/"/>
    
  </entry>
  
  <entry>
    <title>how to get better at things you care about</title>
    <link href="http://lupeipei.github.io/2019/07/09/how-to-get-better-at-things-you-care-about/"/>
    <id>http://lupeipei.github.io/2019/07/09/how-to-get-better-at-things-you-care-about/</id>
    <published>2019-07-09T14:01:26.000Z</published>
    <updated>2019-07-10T01:13:20.519Z</updated>
    
    <content type="html"><![CDATA[<p>TED：<a href="https://www.ted.com/talks/eduardo_briceno_how_to_get_better_at_the_things_you_care_about" target="_blank" rel="noopener">how to get better at things you care about</a></p><a id="more"></a> <p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tKfTcgy1ftigz39kylj317g0mw406.jpg" alt=""></p><h4 id="Reason"><a href="#Reason" class="headerlink" title="Reason"></a>Reason</h4><p>Why we don’t  improve much despite our hard work?</p><p>we tend to spend almost all of our time in the performance zone.</p><h4 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h4><p>Spending more time on learning zone.</p><h4 id="How"><a href="#How" class="headerlink" title="How"></a>How</h4><ul><li>believe and understand that we can improve [growth mindset] </li><li>we must want to improve at that particular skill</li><li>we must have an idea about how to improve [doing deliberate practice]</li><li>we must be in a low-stakes situation</li></ul><p>Real confidence is about modeling ongoing learning.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;TED：&lt;a href=&quot;https://www.ted.com/talks/eduardo_briceno_how_to_get_better_at_the_things_you_care_about&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;how to get better at things you care about&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="Learning" scheme="http://lupeipei.github.io/categories/Learning/"/>
    
    
      <category term="TED" scheme="http://lupeipei.github.io/tags/TED/"/>
    
  </entry>
  
  <entry>
    <title>近期出错小记</title>
    <link href="http://lupeipei.github.io/2019/07/07/recent-error-recording-sets/"/>
    <id>http://lupeipei.github.io/2019/07/07/recent-error-recording-sets/</id>
    <published>2019-07-07T13:34:05.000Z</published>
    <updated>2019-07-08T09:19:23.737Z</updated>
    
    <content type="html"><![CDATA[<p>记录下近期遇到的一些问题以及所犯的低级错误，考虑每个单独写一篇内容太少，索性一锅炖。</p><a id="more"></a> <h4 id="Rspec-针对model-的attribute进行-stub"><a href="#Rspec-针对model-的attribute进行-stub" class="headerlink" title="Rspec 针对model 的attribute进行 stub"></a>Rspec 针对model 的attribute进行 stub</h4><p>一个简单的测试例子：</p><p>有model  Payment,  含字段channel，string 类型。</p><pre><code class="ruby">  it &#39;test&#39; do    allow_any_instance_of(Payment).to receive(:channel).and_return &#39;wxpay&#39;    a = Payment.new    expect(a.channel).to eql &#39;wxpay&#39;  end</code></pre><p>看着没什么问题，但是终端跑测试，报错：</p><pre><code>Article does not implement #title</code></pre><p>参考<a href="https://stackoverflow.com/questions/40535640/rails-rspec-allow-any-instance-of-raises-does-not-implement-error-for-activere" target="_blank" rel="noopener">Rails Rspec allow_any_instance_of raises “does not implement” error for ActiveRecord object attributes</a></p><p>有两种解决方法：</p><ul><li>将<code>allow_any_instance_of(Payment)</code>改为 <code>allow_any_instance_of(Payment.new.class)</code> 【原因看这里<a href="https://relishapp.com/rspec/rspec-mocks/v/3-2/docs/verifying-doubles/dynamic-classes" target="_blank" rel="noopener">Dynamic classes</a>】</li><li>改用<code>instance_double(&#39;Payment&#39;, channel: &#39;wxpay&#39;)</code> ， 替换掉<code>allow_any_instance_of .....</code></li></ul><p>修改测试代码【这里用instance_double】：</p><pre><code class="ruby">  it &#39;test&#39; do    instance_double(&#39;Payment&#39;, channel: &#39;wxpay&#39;)    # 或者 allow_any_instance_of(Payment.new.class).to receive(:channel).and_return &#39;wxpay&#39;    a = Payment.new    expect(a.channel).to eql &#39;wxpay&#39;  end</code></pre><p>OK，passed.</p><h4 id="PostgreSQL-中对array类型的attr查询"><a href="#PostgreSQL-中对array类型的attr查询" class="headerlink" title="PostgreSQL 中对array类型的attr查询"></a>PostgreSQL 中对array类型的attr查询</h4><p>场景：</p><p>model product 中有一属性 categories，数据类型为array.</p><p>现有如下数据：</p><pre><code> product1.categories = [&#39;aa&#39;, &#39;bb&#39;] product2.categories = [&#39;cc&#39;] product3.categories = [&#39;dd&#39;, &#39;aa&#39;] </code></pre><p>给定字符串S, 比如 ‘aa’，如何查询出Product中所有类别包含’aa’的记录？ [肉眼看出，是product1,product3]</p><p>在<code>product.rb</code>中的添加一个scope：</p><pre><code class="ruby">  scope :categroy_cont, -&gt; (categroy) { where(&quot;? = ANY(categories)&quot;, categroy)}</code></pre><p>执行<code>Product.all.category_cont(&#39;aa&#39;)</code> 即可。</p><h4 id="复制整个文件夹，丢失权限和links"><a href="#复制整个文件夹，丢失权限和links" class="headerlink" title="复制整个文件夹，丢失权限和links"></a>复制整个文件夹，丢失权限和links</h4><p>之前复制了一个Rails项目，终端执行：</p><pre><code>bin/yarn xxxx</code></pre><p>报错，显示 <strong>permission denied: bin/yarn</strong> </p><p>终端执行： </p><pre><code>ls -al bin</code></pre><p>bin下面有yarn文件，但是文件属性中没有可执行的权限，, 执行：</p><pre><code>chmod +x bin/*</code></pre><p>解决了。</p><p>当时只知道copy 文件的时候，文件的权限遗失，但是不知道为什么会出现这种情况。</p><p>后面偶然发现了原因，手贱啊，原来是自己当时手动复制了整个文件夹，然后重命名了复制的那个文件夹，结果导致新项目中的link和可执行权限全部丢失了。</p><p>正确的复制方式，保留link 和权限：</p><pre><code class="shell">cp -a fold1 fold2</code></pre><p>这样复制后，得到的fold2下，所有的权限和link都不会丢失。</p><h4 id="git-文件名大小写"><a href="#git-文件名大小写" class="headerlink" title="git 文件名大小写"></a>git 文件名大小写</h4><p>记录下这个，纯粹是因为在大小写这个问题上踩的坑有些过分了。</p><p>直接使用命令：</p><pre><code class="shell">git mv oldfileName newfileName</code></pre><p>比如文件名为Hello.rb， 修改为hello.rb:</p><pre><code>git mv Hello.rb hello.rb</code></pre><p>或者稍微麻烦一点：</p><p>先将文件重命名为Hello1.rb，提交，commit，然后再重命名为hello.rb，再提交，最后将两个commit合并 rebase成一个commit。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://github.com/rspec/rspec-rails/issues/1357" target="_blank" rel="noopener">Dynamic method verification fails when method stubbed using allow_any_instance_of on a class not previously instantiated</a></p><p><a href="https://relishapp.com/rspec/rspec-mocks/v/3-2/docs/verifying-doubles/dynamic-classes" target="_blank" rel="noopener">Dynamic classes</a></p><p><a href="https://stackoverflow.com/questions/40535640/rails-rspec-allow-any-instance-of-raises-does-not-implement-error-for-activere" target="_blank" rel="noopener">Rails Rspec allow_any_instance_of raises “does not implement” error for ActiveRecord object attributes</a></p><p><a href="https://blog.sundaycoding.com/blog/2017/01/22/rails-plus-postgres-array-plus-any-like/" target="_blank" rel="noopener">Rails + Postgres Array + ANY LIKE</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;记录下近期遇到的一些问题以及所犯的低级错误，考虑每个单独写一篇内容太少，索性一锅炖。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Rails" scheme="http://lupeipei.github.io/tags/Rails/"/>
    
      <category term="PostgreSQL" scheme="http://lupeipei.github.io/tags/PostgreSQL/"/>
    
      <category term="ErrorTracking" scheme="http://lupeipei.github.io/tags/ErrorTracking/"/>
    
  </entry>
  
  <entry>
    <title>Mac下安装虚拟机跑window10</title>
    <link href="http://lupeipei.github.io/2019/06/27/install-windows-10-in-vm-that-installed-on-mac/"/>
    <id>http://lupeipei.github.io/2019/06/27/install-windows-10-in-vm-that-installed-on-mac/</id>
    <published>2019-06-27T09:54:06.000Z</published>
    <updated>2019-06-29T14:24:17.173Z</updated>
    
    <content type="html"><![CDATA[<p>N年前装过一次，不记得了，这次又折腾了次，记录下具体步骤。</p><a id="more"></a> <h4 id="操作步骤"><a href="#操作步骤" class="headerlink" title="操作步骤"></a>操作步骤</h4><ul><li><p>下载 <a href="https://www.microsoft.com/en-us/software-download/windows10ISO" target="_blank" rel="noopener">window 10 ISO</a>, 推荐下载 64-bit</p></li><li><p>下载 <a href="https://www.virtualbox.org/wiki/Downloads" target="_blank" rel="noopener">virtual box</a></p></li><li><p>打开下载好的VirtualBox 安装文件，按照指示安装即可</p></li><li><p>安装好 VirtualBox 后，在 launchpad 中找到 VirtualBox, 打开后，新建一个 VM实例，这里选Windows 10 64-bit</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjq7605j318i0pqdl2.jpg" alt="image-20190627163250101"></p></li><li><p>选择 memory size, 点击 continue即可。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjqr8zpj31260mm0vm.jpg" alt="image-20190627163543078"></p></li><li><p>Hard disk:</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjoc7taj310w0ksn1a.jpg" alt="image-20190627163742261"></p></li><li><p>hard disk file type:</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjp92odj31740rgdjp.jpg" alt="image-20190627163810912"></p><p>点击Continue即可。</p></li><li><p>将下载的 window 10 ISO 添加到新建的VM实例中</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjouzr4j30zj0u00ym.jpg" alt="image-20190627164403672"></p><p>弹出的窗口中，选择 <strong>choose disk</strong>，添加 下载的 window 10 ISO 即可。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjo4fkmj31460r2jwj.jpg" alt="image-20190627175852503"></p></li></ul><ul><li><p>最后开启新建的VM实例，会看到如下的 Windows 界面，按照步骤安装即可，enjoy。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjps509j310h0u0q96.jpg" alt="image-20190627165729643"></p></li></ul><h4 id="遇到的问题"><a href="#遇到的问题" class="headerlink" title="遇到的问题"></a>遇到的问题</h4><p>打开 VirtualBox dmg 文件时，安装失败，报错：The installation failed。</p><p>参考 <a href="https://medium.com/@DMeechan/fixing-the-installation-failed-virtualbox-error-on-mac-high-sierra-7c421362b5b5" target="_blank" rel="noopener">Fixing ‘The Installation Failed’ VirtualBox Error on Mac High Sierra</a>。</p><p>打开 system Preferences -&gt; Security &amp; Privacy，当前界面中会有一行类似这样的文字：</p><pre><code>System software from developer “Oracle America, Inc.” was blocked</code></pre><p>旁边有一个按钮 allow，点击即可。</p><p>【忘记截图了，补上的图里面已经没有报错信息，大致位置在allow apps downloaded from  下面，如果没有出现这句话，建议重启 Mac】</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g4fvjr65m3j30y70u0776.jpg" alt="image-20190627162200081"></p><h4 id="更新-2019-06-29-："><a href="#更新-2019-06-29-：" class="headerlink" title="更新[2019-06-29]："></a>更新[2019-06-29]：</h4><p>用的时候才发现开个网页都卡到怀疑人生。<br>果断弃坑，换了<a href="https://www.vmware.com" target="_blank" rel="noopener">VMware</a>, 推荐<a href="https://www.vmware.com/products/fusion/fusion-evaluation.html" target="_blank" rel="noopener">VMware Fusion</a>，可试用一个月，安装起来也很简单，傻瓜式操作，这里就不细说了。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://www.extremetech.com/computing/198427-how-to-install-windows-10-in-a-virtual-machine" target="_blank" rel="noopener">How to install Windows 10 in a virtual machine</a></p><p> <a href="https://medium.com/@DMeechan/fixing-the-installation-failed-virtualbox-error-on-mac-high-sierra-7c421362b5b5" target="_blank" rel="noopener">Fixing ‘The Installation Failed’ VirtualBox Error on Mac High Sierra</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;N年前装过一次，不记得了，这次又折腾了次，记录下具体步骤。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Mac" scheme="http://lupeipei.github.io/tags/Mac/"/>
    
      <category term="VM" scheme="http://lupeipei.github.io/tags/VM/"/>
    
  </entry>
  
  <entry>
    <title>读《许三观卖血记》</title>
    <link href="http://lupeipei.github.io/2019/06/24/notes-of-xu-sanguan-sells-blood/"/>
    <id>http://lupeipei.github.io/2019/06/24/notes-of-xu-sanguan-sells-blood/</id>
    <published>2019-06-24T06:10:44.000Z</published>
    <updated>2019-06-24T06:13:51.409Z</updated>
    
    <content type="html"><![CDATA[<p>“一盘炒猪肝，二两黄酒，黄酒给我温一温……”</p><a id="more"></a> <p>故事的主人许三观，是丝厂的一名工人。</p><p>二十多岁的时候，许三观出于好奇，第一次卖血，卖血换来的钱他用来娶了媳妇，他媳妇叫许玉兰，是城里的”油条西施”。</p><p>婚后五年，许玉兰生了一乐，二乐，三乐，从一乐的相貌中，许三观怀疑自己当乌龟了，后面许玉兰告诉了许三观，一乐是何小勇的孩子。</p><p>十年后，许三观第二次卖血，用来赔偿方铁匠儿子的医药费，因为他儿子一乐闯祸，用石头砸伤了方铁匠儿子的脑袋。</p><p>几年后，厂里的女工林芬芳不小心摔伤了腿，许三观去看望她，犯了生活作风上的错误，许三观第三次卖血，然后给林芬芳买了十斤肉骨头，五斤黄豆，两斤绿豆，一斤菊花……</p><p>许三观第四次卖血，正值三年全国大饥荒【1958年—1962年】，卖血的钱，用来带着全家人，去胜利饭店吃了一碗面。「一乐是事后许三观带着去吃面条的，所以说带着全家人去吃了碗面也不算过分」</p><p>继上次卖血两年后，许三观这一辈子唯一的仇人何小勇被卡车撞伤，此时一乐十三岁，借儿喊魂无效，何小勇过世。</p><p>第五次卖血，在一乐上山下乡，回家探亲，回农村的路上，许三观把卖血得来的钱，给了一乐，让他和二乐补补身体，给生产队长送烟酒，争取早点抽调回城。</p><p>一乐返乡后，一个月不到，许三观第六次卖血，请二乐的生产队长吃饭。【不得不说，这个生产队长真的是让人好生厌恶】</p><p>随后不久，一乐肺炎送去上海治疗，许三观开始了他人生中最集中的一次卖血，在去上海的沿途中，择地卖血。</p><p>此时的许三观，年近五十，先是在林浦卖血400毫升，3天后，他来到了百里，又卖血400毫升，4天后，松林卖血400毫升，晕倒，输血700毫升，路上遇到来喜，来顺，到黄店卖血400毫升，在七里堡，买了来喜200毫升的血，在长宁，卖血400毫升。这趟旅程，共卖血5次，输血2次。</p><p>十多年后，年过六十的许三观因为想要吃炒猪肝，喝黄酒，去医院卖血，被拒。</p><p>稍微统计了下，整本书里，许三观卖血11次，买血2次。</p><pre><code>11 * 400 毫升 - 700 毫升 - 200 毫升 = 3500 毫升</code></pre><p>这样看，许三观卖血共 8.75次【3500/400】。「我也不知道算这个有啥意义，只是纯粹想要统计下。」</p><p>总的来说，许三观是个好人。</p><p>何小勇被卡车撞后，虽然他嘴上刻薄，但还是让一乐去给何小勇喊魂，文革时，他给被批斗的媳妇送饭，家庭批斗的时候，他坦诚自己在生活上所犯的错误。连在医院输血救命的时候，还要把多出来的300毫升血还给医院。</p><p>结尾处，衣食无忧的许三观要去卖血，让人不禁想到《肖生克的救赎》里面那个监狱的图书管理员。</p><p>想了下许三观卖血的行为模式。</p><p>第一次卖血尝到了甜头，刺激神经元，卖血和钱之间产生了连接，卖血 =&gt; 钱，关键这钱来得快且多。以致于后面只要遇到缺钱的时候，他会不自觉的想要调用这个模式，缺钱 =&gt; 卖血 ，连带着卖血后的行为，卖血 =&gt; 炒猪肝喝黄酒。好比有三个神经元，缺钱 =&gt;  卖血 =&gt; 炒猪肝喝黄酒，每次调用这个模式，就是在刺激神经元，促使神经元之间的连接更加牢固，所以炒猪肝是许三观最爱的菜，因为卖血后拿到钱，会产生解决困境的快乐，用炒猪肝来奖励自己，这也是为什么年过六十的许三观想吃炒猪肝，喝黄酒时，非要先去医院卖血。</p><p>嗯，我用自己想到的方式自圆其说了。</p><p>有个地方觉得很疑惑：</p><p>第一次卖血的时候，许三观二十多岁，最后一次卖血的时候，许三观将近50岁了，这二十多年来，卖血的价格都是35块，不增不减，难道他们经历的年代没有通货膨胀？可是大饥荒时代不是通货膨胀很厉害的吗？</p><p>说是长篇小说，但是整本书篇幅并不长，一个下午绰绰有余，有些地方写得还是蛮好玩的，不过觉得有些地方略显浮夸和刻意了，相比余华，还是觉得路遥写的人物更入味。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;“一盘炒猪肝，二两黄酒，黄酒给我温一温……”&lt;/p&gt;
    
    </summary>
    
      <category term="Reading" scheme="http://lupeipei.github.io/categories/Reading/"/>
    
    
      <category term="Reading" scheme="http://lupeipei.github.io/tags/Reading/"/>
    
  </entry>
  
  <entry>
    <title>Nuxt中引入JS脚本报错 - jQuery requires a window with a document</title>
    <link href="http://lupeipei.github.io/2019/06/15/error-recording-nuxt-jquery-requires-a-window-with-a-document/"/>
    <id>http://lupeipei.github.io/2019/06/15/error-recording-nuxt-jquery-requires-a-window-with-a-document/</id>
    <published>2019-06-15T14:37:13.000Z</published>
    <updated>2019-06-15T14:43:45.759Z</updated>
    
    <content type="html"><![CDATA[<p>不止一次遇到这个问题了。</p><a id="more"></a><p>在nuxt的issue <a href="https://github.com/nuxt/nuxt.js/issues/356" target="_blank" rel="noopener">It’s not clear how to include jQuery code #356</a>里面看到了类似情况，不过这位smth仁兄最后选择了放弃，改用directives，可惜这个issue已经closed，不然可以用我蹩脚的英语来码楼……</p><p>这里记录下我的解决方法。</p><h4 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h4><p>在plugins下，添加了JS文件，该文件中引入了 jQuery，定义了多个函数，函数中包含了DOM操作。</p><p>比如这样的一个helper.js 文件：</p><pre><code class="javascript">// plugins/helper.jsimport $ from &#39;jquery&#39;export default {    methods: {        saySomething(words) {      const element = $(`&lt;h2&gt;${content}&lt;/h2&gt;`)      ......    }    }}</code></pre><p>在组件中调用了该文件中的方法 saySomething，页面报出 <strong>jQuery requires a window with a document</strong> 的错误。</p><h4 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h4><p>根据<a href="https://zh.nuxtjs.org/faq/window-document-undefined/" target="_blank" rel="noopener">Window 或 Document 对象未定义？</a>所说的：</p><pre><code>一些只兼容客户端的脚本被打包进了服务端的执行脚本中去。 对于只适合在客户端运行的脚本，需要通过使用 process.client 变量来判断导入</code></pre><p>所以解决方法就很简单了，给saySomething添加上判断可执行的条件即可。</p><p>修改helper.js的saySomething：</p><pre><code class="javascript">// plugins/helper.jsimport $ from &#39;jquery&#39;export default {    methods: {        saySomething(words) {      if (process.client) {        const element = $(`&lt;h2&gt;${content}&lt;/h2&gt;`)          ......      }      }    }}</code></pre><p>OK.</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://zh.nuxtjs.org/faq/window-document-undefined/" target="_blank" rel="noopener">Window 或 Document 对象未定义？</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;不止一次遇到这个问题了。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Nuxt" scheme="http://lupeipei.github.io/tags/Nuxt/"/>
    
      <category term="ErrorTracking" scheme="http://lupeipei.github.io/tags/ErrorTracking/"/>
    
  </entry>
  
  <entry>
    <title>hexo图床迁移及页面404修复</title>
    <link href="http://lupeipei.github.io/2019/06/09/hexo-images-migration-and-page-404-error/"/>
    <id>http://lupeipei.github.io/2019/06/09/hexo-images-migration-and-page-404-error/</id>
    <published>2019-06-09T05:09:58.000Z</published>
    <updated>2019-06-09T05:13:17.938Z</updated>
    
    <content type="html"><![CDATA[<p>趁着放假，整了下hexo，顺带把之前遇到的一些问题一起倒腾了下，记录下两个比较费时的问题及相应解决方法。</p><a id="more"></a> <h4 id="图床迁移"><a href="#图床迁移" class="headerlink" title="图床迁移"></a>图床迁移</h4><p>问题描述：</p><p>图片无法显示：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/Snip20190606_14.png" alt=""></p><p>解决方法：</p><p>图片链接没有问题，在空白页面可以访问，在blog上无法访问，显示403，forbidden。用的是微博的图床，Google了后，找到了解决方法。</p><p>参考<a href="https://www.playpi.org/2019042701.html" target="_blank" rel="noopener">解决微博图床防盗链的问题</a>， 作者写得非常详细，基本按照作者说的步骤来，没啥毛病，稍微卡住的地方在使用插件<a href="https://github.com/PicGo/picgo-plugin-pic-migrater" target="_blank" rel="noopener">picgo-plugin-pic-migrater</a> 的时候，后面发现是自己手贱设置了代理导致。另，作者推荐了图片压缩软件Iimage，这里我用了<a href="https://imageoptim.com/mac" target="_blank" rel="noopener">ImageOptim</a>，小巧方便，推荐。</p><h4 id="部分页面出现404"><a href="#部分页面出现404" class="headerlink" title="部分页面出现404"></a>部分页面出现404</h4><p>问题描述：</p><p>本地正常，但是部署后，发现<strong>部分页面</strong>出现404。</p><p>注意，这里是部分页面，大多数的页面都是可以正常访问的，导致该问题出现的原因，极有可能是GitHub上文件名和本地的文件名存在大小写差异。</p><p>解决方法：</p><p>第一个想法就是git mv，可惜尝试后，完全无效，改过后，终端显示.deploy_git (untracked content)。</p><p>好在Google到了这篇文章 <a href="http://timnew.me/blog/2014/08/20/hexo-file-name-escape-issue-and-solution/" target="_blank" rel="noopener">Hexo file name escape issue and solution</a>，作者给出的方法简单粗暴，但是没关系，有效就好😁。</p><p>终端执行：</p><pre><code class="shell">cd .deploy_gitgit rm -rf *git add .git commit -m &quot;clean all files&quot;cd ..hexo cleanhexo g -d</code></pre><p>此时，GitHub上的文件已经完全同本地新生成的.deploy_git下的文件一样，完美解决。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://www.playpi.org/2019042701.html" target="_blank" rel="noopener">解决微博图床防盗链的问题</a></p><p><a href="https://github.com/Molunerfinn/PicGo" target="_blank" rel="noopener">PIcGo</a></p><p><a href="https://github.com/PicGo/picgo-plugin-pic-migrater" target="_blank" rel="noopener">picgo-plugin-pic-migrater</a> </p><p><a href="http://timnew.me/blog/2014/08/20/hexo-file-name-escape-issue-and-solution/" target="_blank" rel="noopener">Hexo file name escape issue and solution</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;趁着放假，整了下hexo，顺带把之前遇到的一些问题一起倒腾了下，记录下两个比较费时的问题及相应解决方法。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Hexo" scheme="http://lupeipei.github.io/tags/Hexo/"/>
    
      <category term="ErrorTracking" scheme="http://lupeipei.github.io/tags/ErrorTracking/"/>
    
  </entry>
  
  <entry>
    <title>ErrorTracking - Rails 本地无法访问subdomain lvh.me</title>
    <link href="http://lupeipei.github.io/2019/06/08/error-recording-visit-lvh-me-on-rails/"/>
    <id>http://lupeipei.github.io/2019/06/08/error-recording-visit-lvh-me-on-rails/</id>
    <published>2019-06-08T04:18:12.089Z</published>
    <updated>2019-06-08T07:13:28.529Z</updated>
    
    <content type="html"><![CDATA[<p>遇到的一个非常奇葩的错误，记录下。</p><a id="more"></a> <h4 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h4><p>chrome访问页面：</p><pre><code>http://lvh.me:3000</code></pre><p> 查看终端rails s，没有任何报错，也没有任何请求。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g3q5c5k68nj31de0pajsj.jpg" alt=""></p><p>以为是浏览器问题，换Safari，依然无法访问，</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g3q5c73fs0j31ck0psgn7.jpg" alt=""></p><p>用 postman 访问<code>api.lvh.me:3000</code>，也是一样挂掉了。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g3q5c7jpqfj31dg0hc412.jpg" alt=""></p><p>但是如果将lvh.me换成localhost，则上述问题都不存在了，一切正常。</p><p>既然访问localhost:3000正常，难道 lvh.me 没有按照预设被解析成localhost ？</p><p>Google到这篇<a href="https://gist.github.com/indiesquidge/b836647f851179589765" target="_blank" rel="noopener">Subdomaining Localhost with Rails 5</a>，试着给chrome换代理，然后就可以访问了。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g3q5c6ht6rj31qw0bwjvm.jpg" alt=""></p><p>考虑到Postman里面报错显示有一条 Proxy configured incorrectly。</p><p>初步判断是 lvh.me 走了代理导致的，设置lvh.me不走代理即可，考虑到不可能每一个浏览器都做这样的处理，Google了下，可以设置下系统代理。</p><h4 id="最终解决方法："><a href="#最终解决方法：" class="headerlink" title="最终解决方法："></a>最终解决方法：</h4><p>Mac下，打开 system preferences =&gt; network =&gt; advanced =&gt; Proxies， 在下方的Bypass proxy settings for these Hosts &amp; Domains，添加上<code>, *.lvh.me</code>，点击OK 退出，然后点击 apply 即可。</p><p>此时，所有的访问请求都正常了。</p><p>如果chrome依然无法显示页面，退出chrome，重新打开chrome即可。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="http://fionavoss.blog/2018/10/25/subdomains-in-rails/" target="_blank" rel="noopener">Testing subdomains in Ruby on Rails, with and without lvh.me</a></p><p><a href="https://gist.github.com/indiesquidge/b836647f851179589765" target="_blank" rel="noopener">Subdomaining Localhost with Rails 5</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;遇到的一个非常奇葩的错误，记录下。&lt;/p&gt;
    
    </summary>
    
    
      <category term="Rails" scheme="http://lupeipei.github.io/tags/Rails/"/>
    
      <category term="ErrorTracking" scheme="http://lupeipei.github.io/tags/ErrorTracking/"/>
    
  </entry>
  
  <entry>
    <title>读《德伯家的苔丝》</title>
    <link href="http://lupeipei.github.io/2019/06/07/notes-of-tess-of-the-d-Urbervilles/"/>
    <id>http://lupeipei.github.io/2019/06/07/notes-of-tess-of-the-d-Urbervilles/</id>
    <published>2019-06-07T13:15:26.000Z</published>
    <updated>2019-06-09T05:12:40.969Z</updated>
    
    <content type="html"><![CDATA[<p>哈代的长篇小说。</p><a id="more"></a> <p>整本书可以一句话概括为：细谈纯洁天真的少女苔丝如何一步步成为手刃恶棍的杀人犯。</p><p>说笑了，这不是什么女性成长史，这是个悲剧故事。</p><p>大致的故事情节简单说一下：</p><p>女主苔丝，小户人家的大女儿，父亲德伯是个酗酒的懒散小买卖人，偶然从牧师那里得知自己是一个古老的骑士世家的后裔，便自觉血统高贵，与众不同，可惜他没能继承父辈的自强习性，想到的不是通过自己的努力去摆脱困境，改善家中的窘况，反而和妻子盘算着让女儿去附近一个富有的同姓德伯家认亲戚，奢望能攀上一门亲事。</p><p>可惜这个德伯家，却是一个冒牌的世家，靠放高利贷起家，家中掌事的是位老太太，这位老太太有个儿子名德贝维尔，风流浪荡，淫乱无度，在苔丝来到他们家中工作了三个月后，这位浪荡子便染指了苔丝。</p><p>失身后的苔丝悲伤愤怒，回到了自己的家中，数月后生下了一名婴儿，婴儿不久后夭折。</p><p>在家中度过了数年足不出户的日子后，苔丝去了离家较远的一个奶牛场，成为了一个挤奶的女工，开始了新的生活，在这里她与26岁的克莱尔重逢，两人互相爱慕，克莱儿决定向她求婚，新婚当夜，苔丝向克莱儿坦白了自己的过往，克莱儿一时无法接受，决然与苔丝分开，被抛弃的苔丝回到了自己的家中，而克莱尔则远赴巴西。</p><p>婚后的一年多时间，苔丝独自在农场做女工，偶遇了德贝维尔，那个当初侵犯了她的人，如今却做起了牧师，可惜依旧心术不正，德贝维尔屡次骚扰苔丝，要求苔丝成为他的妻子，最后苔丝因家中父亲病故，全家无处安身，而远在巴西的克莱尔也没有及时赶回来帮助她，一时绝望，最终妥协，成为了德贝维尔的情妇。</p><p>这段时间里，在巴西历经磨难的克莱尔也开始后悔自己当初抛弃苔丝的行为，回国后，找寻到了自己的妻子，两人重逢，四目相对，物是人非，苔丝在绝望中，杀死了德贝维尔，而自己则在与克莱尔一起逃亡时，被警察逮捕，处以绞刑。</p><p>结尾部分克莱尔和苔丝的妹妹露一起出现了，两人一起开始了新的生活。</p><p>至此，整本书结束。</p><p>整个故事里，苔丝是无辜的，却也是无可奈何的。</p><p>书中开头的章节里，十六岁的苔丝身穿白色长袍，亭亭玉立，参加会社游行，那是她第一次遇见克莱尔，彼时的她怎会知道这个人会是自己此后人生中的软肋。</p><p>也许从初遇时的那次跳舞，就注定了后面她和克莱尔的结局。</p><p>如果当时苔丝坦白时，克莱尔能接受她的过往，整个故事就都不一样了，可惜悲剧才是整本书的底调。</p><p>对于克莱尔的反应，我其实蛮诧异的，只能说一厢情愿得以为克莱尔思想开明了。要知道克莱尔也向苔丝坦白自己曾在巴黎与一个妇人放荡地生活过，为什么苔丝可以原谅他，而他却不能原谅苔丝？在两人都有的不堪过往中，一个是主动的参与者，另一个却是受害者，很好奇克莱尔评判一个人能否被原谅的标准是什么？凭什么苔丝原谅他的那桩风流往事是理所应当，而他却认为苔丝的过往就不可饶恕了呢？更不用说，他后面抛弃了苔丝，自己远赴巴西之前，还要求当时一个爱慕他的姑娘同他一起前往，他那时早已将婚前他对苔丝的那些种种承诺抛到九霄云外了。</p><p>或许，他自始至终爱的，只是幻想中的苔丝，而不是现实中那个活生生的，有着”污点”的苔丝，看得人委实心寒。</p><p>莫名想起陈医生的那句歌词：如果我露出斑点满身，可马上转身？</p><p>借《刀锋》里面那首诗，送给十六岁那年的苔丝：</p><p>美人儿，我们去看那玫瑰花。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;哈代的长篇小说。&lt;/p&gt;
    
    </summary>
    
      <category term="Reading" scheme="http://lupeipei.github.io/categories/Reading/"/>
    
    
      <category term="Reading" scheme="http://lupeipei.github.io/tags/Reading/"/>
    
  </entry>
  
  <entry>
    <title>读《the little schemer》《the seasoned schemer》</title>
    <link href="http://lupeipei.github.io/2019/05/12/notes-of-the-little-schemer-and-the-seasoned-schemer/"/>
    <id>http://lupeipei.github.io/2019/05/12/notes-of-the-little-schemer-and-the-seasoned-schemer/</id>
    <published>2019-05-12T07:32:54.000Z</published>
    <updated>2019-06-08T04:30:46.691Z</updated>
    
    <content type="html"><![CDATA[<p>终于敲完了两本书的所有示例代码，整个过程还是蛮特别的。</p><a id="more"></a> <h4 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h4><p>看这两本书纯粹是因为之前读了《黑客与画家》，对作者屡次提及的Lisp感到好奇，想要了解下，然后在common lisp和scheme中毫不犹豫选择了后者，仅仅因为它看起来小而巧，而选择这两本scheme书，也是Google到的缘分，现在看来，觉得特别幸运，遇见了这样特别的书。</p><p>书中多处致敬了多位在数学和计算机领域作出贡献的人，还顺带调侃了下《动物庄园》里面的名句，令人捧腹。而往往在你看不懂代码，一头雾水，烦躁不安时，作者又时不时整一两句有关食物的调皮话，让人一下子又觉得没那么难懂了。</p><p>如果所有的编程书都能用这种方式去写，那该多有趣，可惜能把复杂的东西用这样特别的方式呈现出来，对于作者本人的要求着实不低。</p><p>这里简单屡屡两本书的大致内容以及遇到的问题。</p><h4 id="《the-little-schemer》4th"><a href="#《the-little-schemer》4th" class="headerlink" title="《the little schemer》4th"></a>《the little schemer》4th</h4><p>全书10章，入门级，从最基本的概念说起，难度比较大的是chapter09，有关Y-combinator的推导，虽然我目前不是很清楚，Y-combinator到底有怎样特别的用途，但是整个的推导过程并不难，耐着性子跟着作者的思路走就好，而且等你完成这个推导过程后，你会发现读《the seasoned schemer》chapter 16时，Y-bang 已经不再让你感到困惑。</p><p>最后一章chapter 10算是初级版本的scheme解释器，好比手把手教你写一个用scheme解释scheme语法的工具。说它是初级版本，是因为在《the seasoned schemer》chapter 20 中，作者对这块的代码重构了，加入了letcc, set!等，算是终极版本。</p><p>这本书遇到的问题不多，主要是在chapter 6 。</p><p>chapter 6 shadow部分，章节最末尾针对<code>(())</code>，<code>(() ())</code>，<code>(() () ())</code> 类列表定义了函数：+，lat?， 但这两个函数是无效的，执行时，无法输出结果，也许这正是章节标题 shadow的含义。</p><h4 id="《the-seasoned-schemer》"><a href="#《the-seasoned-schemer》" class="headerlink" title="《the seasoned schemer》"></a>《the seasoned schemer》</h4><p>全书10章，是《the little schemer》的进阶版。</p><p>个人觉得这本书相对有些难度的是chapter16和chapter19。</p><p> chapter 16中引入了Y-bang，并定义了一个函数biz用来告诉你它与 Y-combinator其实并不一样，但是具体为何不同，需要自行推导。嗯，你都能推导出Y-combinator了，这个自然也难不倒你，带着肥宅水上路吧！</p><p>chapter 19 中你会看到一个不一样的continuation，真的是很special的一个continuation。反正我自个儿是刷了两三遍才get到two-in-a-raw* 为什么可以那样定义，看完这章的时候，还想着整理点continuation的用法记录下来，结果看完wiki上有关<a href="https://en.wikipedia.org/wiki/Call-with-current-continuation" target="_blank" rel="noopener">call-with-current-continuation</a> 举出的示例代码后，深深觉得自己水平太低，对yin-yang puzzle那块更是懵逼的一塌糊涂，先埋个坑吧，期待日后也能写一写阴阳谜题。</p><p>chapter 20 呼应着《the little schemer》chapter 10，完善了原先的代码，如果你一路走下来对每一章都理解的七七八八，这一章看下来会觉得没啥难度，看完不要忘记奖励自己甜点啊。</p><p>这本书遇到的问题主要在chapter13 和 chapter14。</p><p>chapter 13 hop skip jump 部分，书中直接使用的letcc，在mit-scheme中无法使用, 报错：Unbound variable: letcc，因为它没有定义这种缩写形式，建议练习时，遇到letcc的地方，都改用<code>(call-with-current-continuation (lambda ....))</code>. 或者你也可以这样</p><pre><code class="scheme">(define call/cc call-with-current-continuation)</code></pre><p>使用 call/cc 来代替，这样也算是某种简化了。</p><p>chapter 14 let there be games 部分，章节末尾出现了try， 但是那段代码无法正常执行，没有Google到解决方法，书中给出的try解释也让人有点懵逼，至少我觉得 a 中应该出现 x。不过对该章节印象还蛮深刻的，作者不断的改进函数leftmost，算是结结实实体验了一把Refactoring scheme version。</p><p>chapter 20 中，在使用 let 去重定义*const 时出现了重复代码，可能作者急着去party而忘记删除了😂。这个不算问题，算找茬😄。</p><h4 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h4><p>自然是强烈推荐这两本书，第一次发现原来编程书可以写得这么有意思。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;终于敲完了两本书的所有示例代码，整个过程还是蛮特别的。&lt;/p&gt;
    
    </summary>
    
      <category term="Reading" scheme="http://lupeipei.github.io/categories/Reading/"/>
    
    
      <category term="Scheme" scheme="http://lupeipei.github.io/tags/Scheme/"/>
    
      <category term="Reading" scheme="http://lupeipei.github.io/tags/Reading/"/>
    
  </entry>
  
  <entry>
    <title>用GCP搭建SSserver</title>
    <link href="http://lupeipei.github.io/2019/05/10/install-ss-server-on-google-cloud-platform/"/>
    <id>http://lupeipei.github.io/2019/05/10/install-ss-server-on-google-cloud-platform/</id>
    <published>2019-05-10T07:38:08.000Z</published>
    <updated>2019-06-08T04:19:14.851Z</updated>
    
    <content type="html"><![CDATA[<p>翻墙小白的SS搭建笔记。</p><a id="more"></a> <h4 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h4><p>按照网上的这篇<a href="http://godjose.com/2017/06/14/new-article/" target="_blank" rel="noopener">用Google Cloud Platform搭建Shadowsocks服务教程</a> 来搭建小飞机服务，但是操作的过程中踩了点坑，这里记录下在服务器上搭建的详细过程， 以备下次少踩点坑。</p><p>有关申请GCP和创建实例的步骤，这里略去，以下操作步骤基于Ubuntu 18.04.2，主要的命令语句参考<a href="http://godjose.com/2017/06/14/new-article/" target="_blank" rel="noopener">用Google Cloud Platform搭建Shadowsocks服务教程</a>。</p><h4 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h4><p>创建实例后，点击VM实例页面中的SSH按钮，连接到服务器。</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g2u38httrvj31g00f0mzc.jpg" alt=""></p><p>登录服务器后，先获取 root 权限：</p><pre><code>sudo -i</code></pre><p>更新系统：</p><pre><code>apt updateapt upgrade</code></pre><h4 id="启用BBR"><a href="#启用BBR" class="headerlink" title="启用BBR"></a>启用BBR</h4><p>BBR 由 Google 开发，供 Linux 内核的 TCP 协议栈使用，具体我也不甚了解，可以简单理解为它能加速网络传输速度。</p><p>好了，启用吧。</p><p>写入配置：</p><pre><code>echo &quot;net.core.default_qdisc=fq&quot; &gt;&gt; /etc/sysctl.confecho &quot;net.ipv4.tcp_congestion_control=bbr&quot; &gt;&gt; /etc/sysctl.conf</code></pre><p>配置生效：</p><pre><code>sysctl -p</code></pre><p>校验：</p><pre><code>lsmod | grep bbr</code></pre><p>看到输出类似<code>tcp_bbr  20480</code> 这样的内容，则表明启用成功了。</p><h4 id="搭建SS-Server"><a href="#搭建SS-Server" class="headerlink" title="搭建SS Server"></a>搭建SS Server</h4><p>SS Server的安装可以参考官方文档：<a href="https://github.com/shadowsocks/shadowsocks/blob/master/README.md" target="_blank" rel="noopener">shadowsocks</a>.</p><p>更新软件包：</p><pre><code>apt-get update</code></pre><p>安装python-pip：</p><pre><code>apt-get install python-pip</code></pre><p>使用 pip 安装 SS server：</p><pre><code>pip install shadowsocks</code></pre><p>安装成功后，创建配置文件：</p><pre><code>vi /etc/shadowsocks.json</code></pre><p>在文件中添加以下内容：</p><pre><code>{    &quot;server&quot;:&quot;0.0.0.0&quot;,    &quot;server_port&quot;:8388,    &quot;local_address&quot;: &quot;127.0.0.1&quot;,    &quot;local_port&quot;:1080,    &quot;password&quot;:&quot;password&quot;,    &quot;timeout&quot;:300,    &quot;method&quot;:&quot;aes-256-cfb&quot;}</code></pre><p>这里：server_port， password对应的是之后你在Shadowsocks客户端上配置的端口和密码，请自定义。</p><p>保存文件后，启动SS server：</p><pre><code>ssserver -c /etc/shadowsocks.json -d start</code></pre><p>看到输出started，则表示启动成功。</p><p>但是很不幸，我执行这一步时，启动报错，显示：</p><blockquote><p>undefined symbol EVP_CIPHER_CTX_cleanup shadowsocks start failed</p></blockquote><p>参考这篇<a href="http://tplove.info/post/21/" target="_blank" rel="noopener">ShadowSocks2.8.2启动报错”undefined symbol EVP_CIPHER_CTX_cleanup”</a>， 作者指出是由于openssl升级导致，按照文章指示，做了如下操作：</p><p>编辑<code>/usr/local/lib/python2.7/dist-packages/shadowsocks/crypto/openssl.py</code> 文件：</p><p>【这里文件地址可能不大一样，你可以使用find找到<code>shadowsocks/crypto/openssl.py</code>文件】</p><p>使用 vi 打开文件，将文件中所有<strong>EVP_CIPHER_CTX_cleanup</strong>都修改为<strong>EVP_CIPHER_CTX_reset</strong>。</p><p>作者在文中很贴心的指出共有两处，在第52行和111行，感谢作者的分享。</p><p>修改完成后，重新启动SS server即可。</p><p>更多SS server相关命令，参考<a href="https://github.com/shadowsocks/shadowsocks/wiki" target="_blank" rel="noopener">shadowsocks wiki</a>.</p><h4 id="设置开机自启"><a href="#设置开机自启" class="headerlink" title="设置开机自启"></a>设置开机自启</h4><p>创建脚本文件：</p><pre><code>vi /etc/init.d/shadowsocks</code></pre><p>添加如下内容：</p><pre><code class="shell">#!/bin/shstart(){　　　ssserver -c /etc/shadowsocks.json -d start}stop(){　　　ssserver -c /etc/shadowsocks.json -d stop}case &quot;$1&quot; instart)　　　start　　　;;stop)　　　stop　　　;;reload)　　　stop　　　start　　　;;*)      echo &quot;Usage: $0 {start|reload|stop}&quot;      exit 1　　　;;esac</code></pre><p>为该文件添加可执行权限：</p><pre><code>chmod +x /etc/init.d/shadowsocks</code></pre><p>新建shadowsocks.conf:</p><pre><code>vi /etc/init/shadowsocks.conf</code></pre><p>添加如下内容：</p><pre><code>start on (runlevel [2345])stop on (runlevel [016])pre-start script/etc/init.d/shadowsocks startend scriptpost-stop script/etc/init.d/shadowsocks stopend script</code></pre><p>执行：</p><pre><code>update-rc.d shadowsocks defaults</code></pre><p>开机自启设置完成。</p><p>如果开机自启没有成功，请检查下<code>/etc/init.d/shadowsocks</code> 文件，确保脚本中不存在语法错误，比如中文符号。</p><h4 id="防火墙配置"><a href="#防火墙配置" class="headerlink" title="防火墙配置"></a>防火墙配置</h4><p>写这个是因为当时按照教程搭建完SS server后，发现还是翻不了墙，又不知道哪里出了问题，领导帮忙debug了下，修改了防火墙规则，问题解决。</p><p>在GCP 主页中，选择菜单 <strong>VPC 网络</strong> =&gt;<strong>防火墙规则</strong>， <strong>创建防火墙规则</strong>:</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g2utgj3mtaj30u00zwdkd.jpg" alt=""></p><h4 id="查看日志"><a href="#查看日志" class="headerlink" title="查看日志"></a>查看日志</h4><pre><code>less /var/log/shadowsocks.log</code></pre><p>如果日志上没有访问记录，可以考虑查看防火墙状态：</p><pre><code>ufw status</code></pre><p>或者查看端口情况：</p><pre><code>netstat -anp</code></pre><p>判断问题出在哪里。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="http://godjose.com/2017/06/14/new-article/" target="_blank" rel="noopener">用Google Cloud Platform搭建Shadowsocks服务教程</a></p><p><a href="http://tplove.info/post/21/" target="_blank" rel="noopener">ShadowSocks2.8.2启动报错”undefined symbol EVP_CIPHER_CTX_cleanup”</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;翻墙小白的SS搭建笔记。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Ubuntu" scheme="http://lupeipei.github.io/tags/Ubuntu/"/>
    
      <category term="Shadowsocks" scheme="http://lupeipei.github.io/tags/Shadowsocks/"/>
    
  </entry>
  
  <entry>
    <title>读《天龙八部》</title>
    <link href="http://lupeipei.github.io/2019/05/01/notes-of-tianlongbabu/"/>
    <id>http://lupeipei.github.io/2019/05/01/notes-of-tianlongbabu/</id>
    <published>2019-05-01T01:38:57.000Z</published>
    <updated>2019-06-08T04:30:46.694Z</updated>
    
    <content type="html"><![CDATA[<p>看的是三联版，全书五十章回，第一次拜读金庸大师的作品，切切实实感受了下大师的博学。</p><a id="more"></a> <p>大致的情节就不细说了。97黄日华版的《天龙八部》还是比较忠于原著的，演员演技在线，很经典的一版，少室山下的那场大战，三兄弟各带音箱，不论是剧还是书，都看的人大呼过瘾。</p><p>剧中与原著出入有些大的算是无崖子那段，在大理无量山下，跟他一起琴瑟相合，并诞下一女的是李秋水，而不是他的小师妹，电视剧算是帮无崖子圆了个梦。</p><p>书中虚竹的首次出场，也颇让人意外：</p><blockquote><p>这僧人二十五六岁年纪，浓眉大眼，一个大大的鼻子扁平下塌，容貌颇为丑陋，僧袍上打了许多补钉，却甚是干净。</p></blockquote><p>因为对樊少皇扮演的虚竹印象深刻，所以看到这段的时候，压根儿也想不到这个“容貌颇为丑陋”的僧人竟然是那个呆萌呆萌的虚竹啊！</p><p>书中人物众多，看到别人评金庸小说的，有提到段誉和王语嫣，认为段誉爱的不过是无量山下的那尊玉像，而非王姑娘本身，只是王姑娘恰恰同神仙姐姐模样一般，算是类的一个具体实例，又心许表哥慕容复，对他不甚在意，俗话说，得不到的蠢蠢欲动，更让他对王姑娘欲罢不能。</p><p>对段誉的痴情不做评论，毕竟书中论痴情，游坦之才是当之无愧。因为阿紫，面目全非，双腿残废，毫无怨言，甚至为了让心爱之人能重见光明，不惜在虚竹面前自刎以要求换目，他明明知道她对她姐夫情根深种，自己所做的一切也未曾得到阿紫半点正面的反馈，却依然无怨无悔，最大的心愿，竟不过是能成为相伴阿紫左右的一个奴仆。最后随着阿紫跳入雁门关外的深渊之中，也算是求仁得仁了吧。</p><p>有时候想，如果他换一种方式，努力研习《易筋经》，成为像萧峰那般的人物，也许这样反倒能让阿紫对他多份仰慕和心许。可惜，这一生渺渺，没有如果。</p><p>作为书中悲剧性的人物萧峰，不能不提塞上牛羊空许约。</p><p>萧大侠是幸运的，在被万人唾弃之时，遇到了痴痴不悔的阿朱。</p><p>以前看电视剧的时候，以为阿朱是为了救自己的爹爹段正淳才心甘情愿被萧峰一掌打死，看原著才知道，其实真正的原因，不仅仅是为了让萧峰放下心中的仇恨，更是她顾虑到如果萧峰杀了段正淳，大理皇室自然不会善罢甘休，而大理段家的六脉神剑可能会让萧峰有性命之忧。而她最后嘱咐萧峰照顾阿紫，又何尝不是担心他在自己死后自刎？为了自己的心上人，这般用心良苦。无怪后面耶律洪基要赏赐萧峰上千美女时，萧峰只是苦笑，心道：阿朱就是阿朱，四海列国，千秋万代，就只一个阿朱。</p><p>而对阿朱来说，又何尝不是萧大哥就是萧大哥，四海列国，千秋万代，就只一个萧大哥。</p><p>如果萧峰没有在雁门关外自刎，我倒是很希望他能随完颜阿骨打回到女真族的部落，远离厮杀的江湖，牧羊打猎，平平淡淡度过余生，也许这也是阿朱希望他去过的生活。</p><p>书中其他配角人物的描写也很精彩。比如灵气十足的钟灵，惊鸿一面的木婉清，四处留情的段王爷，幡然醒悟的鸠摩智，一生钟情无崖子的童姥，终极大boss扫地僧，收归于西夏一品堂的四大恶人……哈哈，说到四大恶人，很喜欢那个使鳄鱼剪的南海鳄神，挺可爱的，身上有种傻气的执拗，每次看到他出场了，就会觉得很好玩，刚开始被迫认段誉为师，实非本意，但是后面见识到他的凌波微步，内心已然接受了这段师徒关系，虽常常爆粗口骂人，老大不情愿的在人前称呼段誉师父，但是心肠并不坏，算不上真正的大恶人，最后还为了救段誉而被段延庆杀害了。</p><p>整本书里面，最不喜欢的就是包不同了，真的是恨不得分分钟让他闭嘴啊，处处抬杠也就算了，还非得借抬杠来显摆自己，真心hold不住这类人。</p><p>97版的《天龙八部》，片尾曲是周华健的那首《难念的经》，林夕作词，刷这本小说的时候，委实听了不少遍，词中掺着点佛理，林夕果然是写词高手。</p><p>怪大地众生太美丽。</p><p>附上这首歌：</p><iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width="330" height="86" src="//music.163.com/outchain/player?type=2&id=5271858&auto=0&height=66"></iframe>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;看的是三联版，全书五十章回，第一次拜读金庸大师的作品，切切实实感受了下大师的博学。&lt;/p&gt;
    
    </summary>
    
      <category term="Reading" scheme="http://lupeipei.github.io/categories/Reading/"/>
    
    
      <category term="Reading" scheme="http://lupeipei.github.io/tags/Reading/"/>
    
  </entry>
  
  <entry>
    <title>Vue中嵌套slot的使用</title>
    <link href="http://lupeipei.github.io/2019/04/26/nested-slot-usage/"/>
    <id>http://lupeipei.github.io/2019/04/26/nested-slot-usage/</id>
    <published>2019-04-26T09:06:09.000Z</published>
    <updated>2019-06-08T04:21:36.651Z</updated>
    
    <content type="html"><![CDATA[<p>源于工作中一次组件的重构，这里重新写一个例子来练习下嵌套 slot 的使用。</p><a id="more"></a> <p>嵌套 slot 需要使用到 v-slot 命令，使用该命令需要确保Vue 版本为 2.6.0 +。</p><p>完整的代码可见：<a href="https://gist.github.com/Lupeipei/6175463e9c36eac662ec8261ae969963" target="_blank" rel="noopener">TodoExamples</a>.</p><h4 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h4><h5 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h5><p>现有一个 ToDoList 组件，其子组件 ToDoItem， 组件内容分别如下：</p><pre><code class="vue">&lt;!-- ToDoItem.vue --&gt;&lt;template&gt;  &lt;li&gt;    &lt;span class=&quot;text-muted mr-1&quot;&gt;{{ item.id }}.&lt;/span&gt; {{ item.title }}  &lt;/li&gt;&lt;/template&gt;&lt;script&gt;export default {  props: [ &#39;item&#39; ]}&lt;/script&gt;&lt;!-- ToDoList.vue --&gt;&lt;template&gt;  &lt;ul&gt;    &lt;ToDoItem v-for=&quot;item in items&quot; :key=&quot;item.id&quot; :item=&quot;item&quot; /&gt;  &lt;/ul&gt;&lt;/template&gt;&lt;script&gt;import ToDoItem from &#39;./ToDoItem&#39;export default {  components: { ToDoItem },  props: [ &#39;items&#39; ]}&lt;/script&gt;</code></pre><p>在 schedule 中调用 ToDoList 组件：</p><pre><code class="vue">&lt;!-- schedule.vue --&gt;&lt;template&gt;  &lt;div class=&quot;container&quot;&gt;    &lt;div class=&quot;row&quot;&gt;      &lt;div class=&quot;col-md-6 offset-md-3&quot;&gt;        &lt;ToDoList :items=&quot;items&quot; /&gt;      &lt;/div&gt;    &lt;/div&gt;  &lt;/div&gt;&lt;/template&gt;&lt;script&gt;import ToDoList from &#39;~/components/ToDoList&#39;import _ from &#39;lodash&#39;export default {  components: { ToDoList },  data() {    return {      items: [        { id: 1, title: &#39;todo item 1&#39; },        { id: 2, title: &#39;todo item 2&#39; },        { id: 3, title: &#39;todo item 3&#39; },        { id: 4, title: &#39;todo item 4&#39; }      ],      finished: [ 4, 2 ]    }  }}&lt;/script&gt;</code></pre><p>页面效果如下：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79ly1g2fosn7ml9j314u0fm3yz.jpg" alt=""></p><p>在 schedule 中，有一个数组 finished，里面存放了已经完成的 item 的 ID，现在需要在已经完成的 item 后打上☑️。</p><p>实现如下效果：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79ly1g2fouo0ugpj30ug0fcmxk.jpg" alt=""></p><h4 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h4><p>要实现上述效果，有多个方法，这里采用嵌套 slot 的方式来解决。</p><p>主要思路：通过在 slot 上绑定属性， 让 schedule 层可以访问到 ToDoItem 中的 item，然后通过判断 item 是否在 finished 中出现，决定是否将☑️插入到组件 ToDoItem 中。</p><p>具体操作如下：</p><ul><li><p>给ToDoItem 添加具名插槽 after，同时将组件中的 item 作为 slot 的一个特性绑定上去：</p><p>【将 item 绑定在 slot 上，是为了让父级组件 ToDoList 中的插槽可以访问到该组件中的 item】</p><pre><code class="vue">&lt;!-- ToDoItem.vue --&gt;&lt;template&gt;  &lt;li&gt;    &lt;span class=&quot;text-muted mr-1&quot;&gt;{{ item.id }}.&lt;/span&gt; {{ item.title }}    &lt;slot name=&quot;after&quot; :item=&quot;item&quot; /&gt;  &lt;/li&gt;&lt;/template&gt;&lt;script&gt;export default {  props: [ &#39;item&#39; ]}&lt;/script&gt;</code></pre></li><li><p>在 ToDoList 组件中，引用 ToDoItem 的地方，嵌套 slot，修改 <code>ToDoList.vue</code> 如下：</p><pre><code class="vue">&lt;!-- ToDoList.vue --&gt;&lt;template&gt;  &lt;ul&gt;    &lt;ToDoItem v-for=&quot;item in items&quot; :key=&quot;item.id&quot; :item=&quot;item&quot;&gt;      &lt;template v-slot:after=&quot;{ item }&quot;&gt;        &lt;slot name=&quot;todo-item-after&quot; :item=&quot;item&quot; /&gt;      &lt;/template&gt;    &lt;/ToDoItem&gt;  &lt;/ul&gt;&lt;/template&gt;&lt;script&gt;import ToDoItem from &#39;./ToDoItem&#39;export default {  components: { ToDoItem },  props: [ &#39;items&#39; ]}&lt;/script&gt;</code></pre><p>这里给 ToDoList 添加了一个具名插槽：todo-item-after，同时将 子组件 ToDoItem 中的具名插槽 after 所绑定的特性 item 绑定到 todo-item-after 上，这样外层的 schedule 就可以访问到 ToDoItem 中的 item了。</p></li><li><p>修改 <code>schedule.vue</code>，添加函数 isFinished 用于判断是否需要显示☑️：</p><pre><code class="vue">&lt;!-- schedule.vue --&gt;&lt;template&gt;  &lt;div class=&quot;container&quot;&gt;    &lt;div class=&quot;row&quot;&gt;      &lt;div class=&quot;col-md-6 offset-md-3&quot;&gt;        &lt;ToDoList :items=&quot;items&quot;&gt;          &lt;template v-slot:todo-item-after=&quot;{ item }&quot;&gt;            &lt;i v-if=&quot;isFinished(item)&quot; class=&quot;fas fa-check&quot; /&gt;          &lt;/template&gt;        &lt;/ToDoList&gt;      &lt;/div&gt;    &lt;/div&gt;  &lt;/div&gt;&lt;/template&gt;&lt;script&gt;import ToDoList from &#39;~/components/ToDoList&#39;import _ from &#39;lodash&#39;export default {  components: { ToDoList },  data() {    return {      items: [        { id: 1, title: &#39;todo item 1&#39; },        { id: 2, title: &#39;todo item 2&#39; },        { id: 3, title: &#39;todo item 3&#39; },        { id: 4, title: &#39;todo item 4&#39; }      ],      finished: [ 4, 2 ]    }  },  methods: {    isFinished(item) {      return _.includes(this.finished, item.id)    }  }}&lt;/script&gt;</code></pre><p>如此，便完成了。</p></li></ul><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://cn.vuejs.org/v2/guide/components-slots.html" target="_blank" rel="noopener">插槽</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;源于工作中一次组件的重构，这里重新写一个例子来练习下嵌套 slot 的使用。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Vue" scheme="http://lupeipei.github.io/tags/Vue/"/>
    
  </entry>
  
  <entry>
    <title>对比scheme中的Y-bang和Y</title>
    <link href="http://lupeipei.github.io/2019/04/25/compare-the-difference-between-y-bang-and-y/"/>
    <id>http://lupeipei.github.io/2019/04/25/compare-the-difference-between-y-bang-and-y/</id>
    <published>2019-04-25T10:35:37.000Z</published>
    <updated>2019-06-08T04:13:20.013Z</updated>
    
    <content type="html"><![CDATA[<p>试着找出 Y-bang 和 Y 的不同。</p><a id="more"></a><h4 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h4><p>《the seasoned schemer》chapter16 中提到了Y-bang，后面给到一个biz函数，来比较两者的不同，但是作者赶着去吃炒蛋和煎饼，没有明确指出不同之处在哪里😆。</p><p>用作者的话来说，既然都能推导出Y combinator，自然也就能理解 Y 和 Y-bang 的不同​​。</p><blockquote><p>After you have worked through the definition of the Y combinator, nothing will ever affect your hat size again. not even an attempt to understand the difference between Y and Yi.</p></blockquote><p>既是如此，自己便试着推导了下，算是初步理清了两者的不同。</p><p>开始之前，建议在桌前备上一瓶肥仔水，你会需要它的😆。</p><h4 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h4><p>先看两者的定义：</p><pre><code class="scheme">; define Y-bang;(define Y-bang  (lambda (f)    (letrec      ((h (f (lambda (arg) (h arg)))))      h)));; Y combinator from chapter 09, old friend.;(define Y  (lambda (le)    ((lambda (f)      (f f))      (lambda (f)        (le (lambda (x)              ((f f) x)))))));</code></pre><h4 id="推导过程"><a href="#推导过程" class="headerlink" title="推导过程"></a>推导过程</h4><p>Y-bang的推导较简单，可以参看<a href="https://mitpress.mit.edu/books/seasoned-schemer-second-edition" target="_blank" rel="noopener">《the seasoned schemer》</a>chapter 16 ，这里不妨再过一遍：</p><p>故事依然从length开始：</p><pre><code class="scheme">; define length;(define length  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))));</code></pre><p>使用let 和 set! 来重新定义length:</p><pre><code class="scheme">; using let and set! to get another version of length;(define length  (let ((h (lambda (l) 0)))    (set! h      (lambda (l)        (cond          ((null? l) 0)          (else (add1 (h (cdr l)))))))    h));</code></pre><p>这里我们来定义一个函数L，用来简化这个新版本length的定义:</p><pre><code class="scheme">; define L;(define L  (lambda (length)    (lambda (l)      (cond        ((null? l) 0)        (else (add1 (length (cdr l))))))));</code></pre><p>这个 L 函数，接受一个函数length做为参数，返回结果是一个lambda函数。</p><p>利用 L，来简化 length：</p><pre><code class="scheme">(define length  (let ((h (lambda (l) 0)))    (set! h      (L (lambda (arg) (h arg))))    h))</code></pre><p>如果你看懂了这步，可跳过下面的推导细节。</p><hr><p>为什么可以这样简化？ 我们将 L 换成 定义的lambda函数：</p><pre><code class="scheme">; replace L with its definition((lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l))))))) (lambda (arg) (h arg)));; replace length with (lambda (arg) (h arg)), we can get those below:(lambda (l)    (cond      ((null? l) 0)      (else (add1 ((lambda (arg) (h arg)) (cdr l))))));; the result of ((lambda (arg) (h arg)) (cdr l)) is (h (cdr l));(lambda (l)    (cond      ((null? l) 0)      (else (add1 (h (cdr l))))))</code></pre><p>最后得到的<code>(L (lambda (arg) (h arg)))</code> 的结果就是：</p><pre><code class="scheme">(lambda (l)  (cond   ((null? l) 0)   (else (add1 (h (cdr l))))))</code></pre><p>这时，替换<code>(L (lambda (arg) (h arg)))</code> ，得到的length如下：</p><pre><code class="scheme">(define length  (let ((h (lambda (l) 0)))    (set! h      (lambda (l)        (cond          ((null? l) 0)          (else (add1 (h (cdr l)))))))    h));</code></pre><hr><p>利用新定义的length，可以推导出Y-bang.</p><pre><code class="scheme">; we get Y-bang;(define Y-bang  (lambda (f)    (letrec      ((h (f (lambda (arg) (h arg)))))      h)));</code></pre><p>Y 稍微复杂了些 ，如果你看<a href="https://mitpress.mit.edu/books/seasoned-schemer-second-edition" target="_blank" rel="noopener">《the little schemer》</a>chapter 09 时仍是懵逼状，可参考之前这篇<a href="https://luciaca.cn/2019/04/13/deriving-the-Y-combinator-in-scheme/" target="_blank" rel="noopener">scheme中有关Y combinator 的推导</a>。</p><h4 id="两者对比"><a href="#两者对比" class="headerlink" title="两者对比"></a>两者对比</h4><p>当调用的函数符合一定的”模式”时，两者没什么大的区别。</p><p>看一个depth* 的例子：</p><pre><code class="scheme">; define D;(define D  (lambda (depth*)    (lambda (s)      (cond        ((null? s) 1)        ((atom? (car s)) (depth* (cdr s)))        (else          (max            (add1 (depth* (car s)))            (depth* (cdr s))))))));</code></pre><p>使用Y-bang 和 Y 来定义 depth*:</p><pre><code class="scheme">(define depth* (Y-bang D))(depth* (quote (c (b (a b) a) a)));Value: 3(define depth* (Y D))(depth* (quote (c (b (a b) a) a)));Value: 3</code></pre><p>使用<code>(Y-bang D)</code> 和 <code>(Y D)</code> 来定义 depth*，得到的结果相同。</p><p>来看个不一样的例子：</p><pre><code class="scheme">; define biz;(define biz  (let ((x 0))    (lambda (f)      (set! x (add1 x))      (lambda (a)        (if (= a x)            0            (f a))))))</code></pre><p>调用biz:</p><pre><code class="scheme">((Y biz) 5); Value: 0((Y-bang biz) 5); no answer</code></pre><p>此时<code>((Y-bang biz) 5)</code>没有结果，因为调用<code>((Y-bang biz) 5)</code>时，只执行了一次<code>(set! x (add1 x))</code> ，x 一直是1，进入了一个无限的死循环。</p><p>为什么会是这样的？</p><p>来看一下<code>(Y-bang biz)</code> 是什么：</p><pre><code class="scheme">; define Y-bang;(define Y-bang  (lambda (f)    (letrec      ((h (f (lambda (arg) (h arg)))))      h))); define biz;(define biz  (let ((x 0))    (lambda (f)      (set! x (add1 x))      (lambda (a)        (if (= a x)            0            (f a))))));; now, we want know what (Y-bang biz) is.;; first, replace Y-bang, biz with their definition.;((lambda (f)    (letrec      ((h (f (lambda (arg) (h arg)))))      h)) (let ((x 0))        (lambda (f)          (set! x (add1 x))          (lambda (a)            (if (= a x)                0                (f a))))));; then, replace f with biz&#39;s definition(letrec  ((h ((let ((x 0))          (lambda (f)            (set! x (add1 x))            (lambda (a)              (if (= a x)                  0                  (f a))))) (lambda (arg) (h arg)))))  h);; and, replace f in biz with (lambda (arg) (h arg))(letrec  ((h ((let ((x 0))          (lambda (f)            (set! x (add1 x))            (lambda (a)              (if (= a x)                  0                  ((lambda (arg) (h arg)) a))))))))  h);; the result of ((lambda (arg) (h arg)) a) is (h a); finally, we get it.(letrec  ((h ((let ((x 0))         (set! x (add1 x))         (lambda (a)           (if (= a x)               0               (h a)))))))  h)</code></pre><p>最后得到的结果中，可以看出，第一次调用递归函数 h 时，x 初始化为零，然后执行 add1, x 为1，此后递归时，不再执行 <code>(lambda (a) ....)</code> 外的表达式，x的值一直没变化，保持为1，<code>((Y-bang biz) 5)</code> 自然是无结果的。</p><p>我们可以测试一下，既然 x 的值一直是1，那么<code>((Y-bang biz) 1)</code> 应该有结果, 且为0:</p><pre><code class="scheme">((Y-bang biz) 1); Value: 0</code></pre><p>我们可以修改一下biz函数，让<code>((Y-bang biz) 5)</code> 得到结果0。</p><pre><code class="scheme">; redefine biz;(define biz  (lambda (f)    (let ((x 0))      (lambda (a)        (set! x (add1 x))        (if (= a x)            0            (f a))))));((Y-bang biz) 5); Value: 0</code></pre><p>   至此，<code>（Y-bang biz)</code> 部分结束。</p><p>惭愧的是，一直无法推导出<code>(Y biz)</code> 的最终样子，将biz 的定义带入Y 中时，卡在<code>(f f)</code>这里，简直就是一个无限死循环。</p><p>算是一个未填的坑，留待猴年马月再填。</p><p>初步猜测<code>(Y biz)</code>最终的结果类似这样：</p><pre><code class="scheme">(lambda (a)  (let ((x 0))    (letrec      ((h        (lambda (a)          (set! x (add1 x))          (if (= a x)              0              (h a)))))      (h a))))</code></pre><p>另，这里不推荐使用<a href="http://www.gnu.org/software/mit-scheme/documentation/mit-scheme-user/Debugging-Aids.html" target="_blank" rel="noopener">pp</a> 来输出<code>(Y biz)</code>  和 <code>(Y-bang biz)</code>的结果，因为输出的结果中，两者相同：</p><pre><code class="scheme">(pp (Y biz)); result;(lambda (a)  (if (= a x)      0      (f a)))(pp (Y-bang biz));result;(lambda (a)  (if (= a x)      0      (f a)))</code></pre><p>可以看到，输出的结果中已经弃掉了<code>(set! x (add1 x))</code>， 使得两者看着好像是一样的，但其实并不相同，这样的结果容易误人子弟啊。</p><p>OK，Time for 🍰.</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://mitpress.mit.edu/books/seasoned-schemer-second-edition" target="_blank" rel="noopener">《the seasoned schemer》</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;试着找出 Y-bang 和 Y 的不同。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Scheme" scheme="http://lupeipei.github.io/tags/Scheme/"/>
    
  </entry>
  
  <entry>
    <title>scheme中有关Y combinator 的推导</title>
    <link href="http://lupeipei.github.io/2019/04/13/deriving-the-Y-combinator-in-scheme/"/>
    <id>http://lupeipei.github.io/2019/04/13/deriving-the-Y-combinator-in-scheme/</id>
    <published>2019-04-13T07:07:47.000Z</published>
    <updated>2019-06-08T04:17:21.597Z</updated>
    
    <content type="html"><![CDATA[<p>推导结束，两瓶元气森林已空瓶。</p><a id="more"></a><h4 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h4><p>《the little schemer》中的第9章，可以换一个开头：故事从一个没有名字的递归开始说起……</p><p>鉴于理解的过程蛮苦逼，记录下整个Y combinator 的推导过程，便于下次失忆的时候能迅速回忆。</p><p>不过在推导前，先来看看Y combinator 是什么。</p><h4 id="Y-combinator"><a href="#Y-combinator" class="headerlink" title="Y combinator"></a>Y combinator</h4><p>wiki上的解释  <a href="https://en.wikipedia.org/wiki/Fixed-point_combinator#Fixed_point_combinators_in_lambda_calculus" target="_blank" rel="noopener">fixed-point combinator </a> 是这样的：</p><blockquote><p>fix, when applied to an arbitrary function f, yields the same result as f applied to the result of applying fix to f.</p></blockquote><p>细节部分太复杂了。Google 到这篇 <a href="https://docs.huihoo.com/homepage/shredderyin/wiki/SchemeYcombinator.html" target="_blank" rel="noopener">Y Combinator的scheme 实现</a>， 发现作者的解释看起来清楚多了，可参阅。</p><p>简单而言, 一个公式：</p><blockquote><p>fix f = f (fix f) for all f</p></blockquote><p>对于任意的函数f,  fix 接受参数 f，返回(fix f)，将 f 作用于 (fix f)，得到的结果仍为(fix f)。</p><p>在scheme中，Y 的定义如下：</p><pre><code class="scheme">(define Y  (lambda (le)    ((lambda (f)      (f f))      (lambda (f)        (le (lambda (x)              ((f f) x)))))))</code></pre><p>或者这样：</p><pre><code class="scheme">(define Y (lambda (F)   (let ((W (lambda (x)              (F (lambda arg (apply (x x) arg))))))     (W W))))</code></pre><p>两者其实是一样的。</p><p>来看一个调用Y的实例：</p><p>定义一个函数 Fi，用来计算斐波那契数列：【 F(1)=1，F(2)=1, F(n)=F(n-1)+F(n-2)（n&gt;=3) 】</p><pre><code class="scheme">(define Fi  (lambda (func-arg)    (lambda (n)      (cond        ((zero? (sub1 n)) 1)        ((zero? (- n 2)) 1)        (else (+ (func-arg (sub1 n)) (func-arg (- n 2))))))))</code></pre><p>然后执行：</p><pre><code class="scheme">((Y Fi) 10);Value: 55((Fi (Y Fi)) 10);Value: 55</code></pre><p>两次调用的输出结果相同。</p><h4 id="推导过程"><a href="#推导过程" class="headerlink" title="推导过程"></a>推导过程</h4><p>以书中定义的length函数为例：</p><p>先定义一个永远不会有值输出，死循环的函数：eternity</p><pre><code class="scheme">(define eternity  (lambda (x)    (eternity x)))</code></pre><p>length的定义如下：</p><pre><code class="scheme">(define length  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))))</code></pre><p>这里，我们定义了一个函数，命名为length，而且在该函数内部调用了它自己。</p><p>现在问题来了，如果不允许使用define来定义函数，不给函数命名，如何实现length？</p><p>我们先从最简单的 length0 开始，当 list 为空时，<code>(length l)</code>为 0.</p><p>先来看 length0:</p><pre><code class="scheme">; length0;(lambda (l)  (cond    ((null? l) 0)    (else (add1 (eternity (cdr l))))))</code></pre><p>调用时， 也很简单：</p><pre><code class="scheme">((lambda (l)  (cond    ((null? l) 0)    (else (add1 (eternity (cdr l)))))) (quote ()));Value: 0</code></pre><p>由此，继续定义 length&lt;=1 ：</p><pre><code class="scheme">; length&lt;=1;(lambda (l)  (cond    ((null? l) 0)    (else      (add1 (        (lambda (l)           (cond             ((null? l) 0)             (else (add1 (eternity (cdr l))))))        (cdr l))))))</code></pre><p>调用  length&lt;=1 ：</p><pre><code class="scheme">((lambda (l)  (cond    ((null? l) 0)    (else      (add1 (        (lambda (l)           (cond             ((null? l) 0)             (else (add1 (eternity (cdr l))))))        (cdr l)))))) (quote (hello)));Value: 1</code></pre><p>类推 length&lt;=2：</p><pre><code class="scheme">(lambda (l)  (cond    ((null? l) 0)    (else      (add1 (        (lambda (l)          (cond            ((null? l) 0)            (else              (add1 (                (lambda (l)                   (cond                     ((null? l) 0)                     (else (add1 (eternity (cdr l))))))                (cdr l))))))          (cdr l))))))</code></pre><p>调用  length&lt;=2 ：</p><pre><code class="scheme">((lambda (l)  (cond    ((null? l) 0)    (else      (add1 (        (lambda (l)          (cond            ((null? l) 0)            (else              (add1 (                (lambda (l)                   (cond                     ((null? l) 0)                     (else (add1 (eternity (cdr l))))))                (cdr l))))))          (cdr l)))))) (quote (hello scheme)));Value: 2</code></pre><p>按照这个规律，我们可以一直定义下去，length&lt;=n, n 为无穷大……</p><p>但这显然不是一个函数该有的样子，所以我们换一种方式，来重新定义下 length0:</p><pre><code class="scheme">; redefine length0;((lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))))  eternity)</code></pre><p>这里重新定义了一个函数，这个函数接受一个参数length，返回结果是一个lambda函数。</p><p>上面这段代码，调用了该函数，并将 eternity 作为参数值传递进去。与我们先前定义的 length0 没有区别，仅仅是写法不同而已。</p><p>用这种方式，可以类推出 length&lt;=1, length&lt;=2:</p><pre><code class="scheme">; rewrite length&lt;=1 in the same style;((lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))))  ((lambda (length)    (lambda (l)      (cond        ((null? l) 0)        (else (add1 (length (cdr l))))))) eternity));; rewrite length&lt;=2 in the same style;((lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))))((lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))))  ((lambda (length)    (lambda (l)      (cond        ((null? l) 0)        (else (add1 (length (cdr l))))))) eternity)))</code></pre><p>顺着这个思路，再一次进行抽象提取，重新定义 length0:</p><pre><code class="scheme">; mk-length for length0;((lambda (mk-length)    (mk-length eternity))    (lambda (length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 (length (cdr l))))))))</code></pre><p>这里，是将整个 length 的定义，作为 mk-length 的参数值：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79ly1g260f7p56aj30vo0c4my9.jpg" alt=""></p><p>它其实是同我们第二次定义的 length0 是一样的：</p><p>只是这里调用了两次，一次将整个 length 的定义，作为 mk-length 的参数值，第二次将 eternity 作为 length 的参数值。</p><p>由此可以类推出length&lt;=1, length&lt;=2:</p><pre><code class="scheme">; mk-length for length&lt;=1;((lambda (mk-length)    (mk-length      (mk-length eternity)))  (lambda (length)    (lambda (l)      (cond        ((null? l) 0)        (else (add1 (length (cdr l))))))));; mk-length for length&lt;=2;((lambda (mk-length)  (mk-length    (mk-length      (mk-length eternity))))  (lambda (length)    (lambda (l)      (cond        ((null? l) 0)        (else (add1 (length (cdr l))))))))</code></pre><p>这里，eternity 扮演的是参数值的角色，它本身是一个函数，我们使用 mk-length 来替换掉 eternity，当然也是可以的，重新看一下替换掉eternity后的 length0 :</p><pre><code class="scheme">; replace eternity with mk-length;((lambda (mk-length)    (mk-length mk-length))    (lambda (length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 (length (cdr l))))))))</code></pre><p>这里，length也不过是一个参数名，自然也是可以换成mk-length的。</p><pre><code class="scheme">; using mk-length instead of length;((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 (mk-length (cdr l))))))))</code></pre><p>现在，调用上面这个函数，将eternity作为参数值，我们可以得到 length&lt;=1:</p><pre><code class="scheme">; apply mk-length to get length&lt;=1;((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 ((mk-length eternity) (cdr l))))))))</code></pre><p>这里稍稍有点绕，截个图就明白了：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g20zc49bf6j317q0c4ta9.jpg" alt=""></p><p>调用一次试试看：</p><pre><code class="scheme">(((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 ((mk-length eternity) (cdr l)))))))) (quote (apple)));Value: 1</code></pre><p>结果无误。</p><p>现在，我们再将上面 length&lt;=1定义中 的 eternity 替换成 mk-length:</p><pre><code class="scheme">; replace eternity with mk-length;((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 ((mk-length mk-length) (cdr l))))))))</code></pre><p>鉴于<code>(f x)</code> 与 <code>(lambda (x) (f x))</code> 是一样的，所以我们使用<code>((lambda (x) ((mk-length mk-length) x)) (cdr l))</code> 来替换掉<code>((mk-length mk-length) (cdr l))</code>。</p><p>替换后的结果如下：</p><pre><code class="scheme">((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (lambda (l)        (cond          ((null? l) 0)          (else (add1 ((lambda (x) ((mk-length mk-length) x)) (cdr l))))))))</code></pre><p>再来一次抽取，将<code>(lambda (x) ((mk-length mk-length) x))</code> 作为参数值提取出来：</p><pre><code class="scheme">((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      ((lambda (length)        (lambda (l)          (cond            ((null? l) 0)            (else (add1 (length (cdr l)))))))        (lambda (x) ((mk-length mk-length) x)))))</code></pre><p>你会发现最初定义length的那段代码已经完整浮现了：</p><p><img src="https://raw.githubusercontent.com/Lupeipei/blogImages/master/images/006tNc79gy1g20zkh1gz2j314c0ei75t.jpg" alt=""></p><p>最后一次，将整块length的定义作为参数值，提取出来：</p><pre><code class="scheme">((lambda (le)  ((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (le (lambda (x)            ((mk-length mk-length) x)))))) (lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l))))))))</code></pre><p>这段代码中，将：</p><pre><code class="scheme"> (lambda (length)  (lambda (l)    (cond      ((null? l) 0)      (else (add1 (length (cdr l)))))))</code></pre><p>作为了le的参数值。</p><p>现在，我们移除掉这个参数值，得到如下代码：</p><pre><code class="scheme">(lambda (le)  ((lambda (mk-length)    (mk-length mk-length))    (lambda (mk-length)      (le (lambda (x)            ((mk-length mk-length) x))))))</code></pre><p>好了，我们可以撒花🎉啦。</p><p>用 f 替换 mk-length，Y combinator 登场：</p><pre><code class="scheme">; Y combinator;(define Y  (lambda (le)    ((lambda (f)      (f f))      (lambda (f)        (le (lambda (x)              ((f f) x)))))))</code></pre><p>整个推导过程结束。</p><p>另，<a href="https://github.com/pkrumins" target="_blank" rel="noopener">Peter Krumins</a> 有一篇文章也详细演示了Y combinator的推导过程，可参阅 <a href="https://catonmat.net/derivation-of-ycombinator" target="_blank" rel="noopener">Deriving the Y-Combinator</a>。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://en.wikipedia.org/wiki/Fixed-point_combinator#Fixed_point_combinators_in_lambda_calculus" target="_blank" rel="noopener">fixed-point combinator</a>.  </p><p><a href="https://docs.huihoo.com/homepage/shredderyin/wiki/SchemeYcombinator.html" target="_blank" rel="noopener">Y Combinator的scheme 实现</a></p><p><a href="https://mitpress.mit.edu/books/little-scheme-fourth-edition" target="_blank" rel="noopener">《The little schemer》</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;推导结束，两瓶元气森林已空瓶。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Scheme" scheme="http://lupeipei.github.io/tags/Scheme/"/>
    
  </entry>
  
  <entry>
    <title>对scheme中collector的一点理解</title>
    <link href="http://lupeipei.github.io/2019/04/11/understanding-collector-in-scheme/"/>
    <id>http://lupeipei.github.io/2019/04/11/understanding-collector-in-scheme/</id>
    <published>2019-04-11T05:58:28.000Z</published>
    <updated>2019-06-08T04:36:01.562Z</updated>
    
    <content type="html"><![CDATA[<p>collector出现在《 the little schemer》chapter 08中，刷了两遍才算勉强理解，记录下。</p><a id="more"></a> <h4 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h4><p>collector 的运用，对应着《 the little schemer》中提到了第10戒：<strong>Build functions to collect more than one value at a time.</strong> 简单理解为创建函数，一次收集多个值，然后对这些值进行再处理。</p><p>看个例子，加深对collector的理解。</p><h4 id="代码演示"><a href="#代码演示" class="headerlink" title="代码演示"></a>代码演示</h4><p>根据《the little schemer》chapter 08 中给出的奇偶数例子稍稍加以改编。</p><p>定义函数 <code>number-only*&amp;co</code>，给到一个list， 要求：找出这个list中的所有数字，并求出数字的总和。将总和和新的数字集一并输出。</p><p>比如：</p><pre><code class="scheme">; 输出结果的格式（sum newlist), list的第一个元素是求和的结果，第二个元素为所取到数字集。;(number-only*&amp;co (quote (9 apples 2 oranges 10 peaches 11 pears)) cal-number);Value : (32 (9 2 10 11))(number-only*&amp;co (quote (9 apples (2 oranges) (10) peaches ((11)) pears)) cal-number);Value : (32 (9 (2) (10) ((11))))</code></pre><p>这里<code>cal-number</code> 就是一个collector。</p><p>来看<code>number-only*&amp;co</code> 和 <code>cal-number</code>的定义：</p><pre><code class="scheme">; define number-only*&amp;co;(define number-only*&amp;co  (lambda (l col)    (cond      ((null? l) (col (quote ()) 0))      ((atom? (car l))      (cond        ((number? (car l))         (number-only*&amp;co (cdr l)          (lambda (newlat s)            (col (cons (car l) newlat) (+ (car l) s)))))        (else (number-only*&amp;co (cdr l) col))))      (else (number-only*&amp;co (car l)                           (lambda (al as)                            (number-only*&amp;co (cdr l)                              (lambda (dl ds)                                (col (cons al dl) (+ as ds))))))))));; define cal-number;(define cal-number  (lambda (l s)    (cons s (cons l (quote ())))));</code></pre><p>  简单解释下：</p><p>  number-only*&amp;co 接受两个参数，一个是list，一个是collector。</p><p>  collector接受两个参数，一个是newlist,  一个是 s (s is short for sum)， newlist用来存 l 中的数字，s用来存数字求和的结果。</p><p>  collector的初始值是<code>col((quote ()) 0)</code>，也就是newlist 为空，s为0，理解这一点很重要。</p><ul><li><p>当 l 是空的 list 时，<code>number-only*&amp;co</code>的结果就是 <code>(col (quote ()) 0)</code>.</p></li><li><p>当 l 不为空时，判断 l 的第一个元素是不是一个atom:</p><ul><li><p><code>(atom? (car l))</code> 为 #t, 判断第一个元素是不是number：</p><ul><li><p>第一个元素是number，添加到newlist里面，同时s = s + (car l)。此时新的collector长这样:</p><pre><code class="scheme">(lambda (newlat s)   (col (cons (car l) newlat) (+ (car l) s)))</code></pre></li><li><p>第一个元素不是number，那就什么也不做，collector不变，仍然为col。</p></li></ul></li><li><p><code>(atom? (car l))</code> 为 #f:</p><p>这里就要定义 <code>(car l)</code> 后的collector了。这个collector需要使用<code>number-only*&amp;co</code> 来遍历 <code>(cdr l)</code>，挑出<code>(cdr l)</code>中的所有数字，并算出这些数字的和。所以，它应该长成这样：</p><pre><code class="scheme">(lambda (al as)  (number-only*&amp;co (cdr l)     (lambda (dl ds)          ......)))</code></pre><p>「这里al, as 是 <code>number-only*&amp;co</code>  处理<code>(car l)</code> 时得到的结果，而dl, ds 是 <code>number-only*&amp;co</code> 处理 <code>(cdr l)</code> 时得到的结果。」</p><p>有了al, dl, as, ds， 后面只需要将两者得到的结果整合起来即可。</p><p>所以，else语句中，最后的collector长这样：</p><pre><code class="scheme">(lambda (al as)  (number-only*&amp;co (cdr l)    (lambda (dl ds)      (col (cons al dl) (+ as ds)))))</code></pre></li></ul></li></ul><p>至此， <code>number-only*&amp;co</code>  的定义完成。</p><p>而<code>cal-number</code> 仅仅是一个collector，返回(sum newlist)， 没什么特别的。</p><p>也可以定义新的collector，比如只返回sum，或者只返回newlist。</p><pre><code class="scheme">(define sum-number  (lambda (l s)    s));(define number-list  (lambda (l s)    l))</code></pre><p>然后调用的时候，使用新的collector：</p><pre><code class="scheme">(number-only*&amp;co (quote (9 apples (2 oranges) (10) peaches ((11)) pears)) sum-number);Value: 32(number-only*&amp;co (quote (9 apples (2 oranges) (10) peaches ((11)) pears)) number-list);Value : (9 (2) (10) ((11)))</code></pre><p>OK。算是又刷了一遍，可以开启下一章的旅途了。</p><h4 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h4><p><a href="https://mitpress.mit.edu/books/little-scheme-fourth-edition" target="_blank" rel="noopener">《The little schemer》</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;collector出现在《 the little schemer》chapter 08中，刷了两遍才算勉强理解，记录下。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://lupeipei.github.io/categories/CS/"/>
    
    
      <category term="Scheme" scheme="http://lupeipei.github.io/tags/Scheme/"/>
    
  </entry>
  
</feed>
